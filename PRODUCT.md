# Product

<!-- impeccable:product-schema 1 -->

## Platform

android

## Users

- المستخدم الأساسي: مستمع كتب صوتية عربية، يستمع يوميًا لجلسات طويلة (ساعات)، غالبًا ليلًا (أو في بيئة هادئة يريد فيها راحة العين).
- الدولة/البيئة التشغيلية: واجهة عربية RTL كاملة أولًا وأخيرًا؛ أي مبدأ تصميم يتعارض مع RTL يُعدَّل ليتماشى معه ولا يُطبَّق حرفيًا بقواعد LTR.
- المستخدم يدير مكتبة كتب صوتية لا يكتفي بتشغيل الملفات: يفرز، يفلتر، يبحث بتطبيع عربي، يدير الإصدارات المكررة والمطابقات، ويضع إشارات مرجعية ملاحظات.
- يُعد المستخدم حساسًا بصريًا: التطبيق الحالي وقع في أنماط "AI slop" (تدرج بنفسجي-أزرق، بطاقات رمادية موحدة، مربعات ألوان فارغة كأغلفة) — هذه الأنماط مرفوضة صراحة.

## Product Purpose

تطبيق استماع لكتب صوتية عربية وعنوانه "مدير مكتبة ذكي، ليس مجرد مشغل صوت". الغاية: إدارة مكتبة كتب صوتية (فهرسة مجلدات/ملفات من جهاز المستخدم)، تمييز إصدارات الكتاب الواحد (Edition Detection بذكاء وثقة)، تشغيل صوتي طويل ومريح (Sleep Timer ذكي، حفظ الموضع فورًا، غلاف ديناميكي)، وتتبع تاريخ الاستماع والإحصاءات (Streak، كتب/فصول مكتملة، أوقات الاستماع، متوسط السرعة).

النجاح: مستخدم يشعر أن مكتبته "مفهومة" — تُفهرس تلقائيًا، تُفلتر وتُبحث بذكاء عربي، تُشغَّل بلا انقطاع، ولا يضيع موضعه أو إشاراته أبدًا.

## Positioning

"مكتبة ذكية للكتب الصوتية العربية": المنتج المميز هو اكتشاف الإصدارات (نفس الكتاب في أكثر من مجلد/نسخة، أو ملفات متفرقة) بقرار قائم على إشارات حقيقية (اسم الملف/المجلد، الراوي، المدة، الوسوم الداخلية) ومستوى ذكاء يختاره المستخدم (Conservative/Balanced/Aggressive) — مع "لا دمج تلقائي" حاسم عند اختلاف واضح في الراوي أو فرق مدة > 15%. هذا الادعاء لا يستطيع جاره إعادة إنتاجه لأنه مبني على بنية Data مكتملة (Room Entities + Signal extraction + Decision log) وليس على مسار تجريبي.

## Operating Context

- النظام: Android أصلي (Kotlin + Jetpack Compose + Room + Hilt + Media3/ExoPlayer). RTL إلزامي.
- المكتبة الفعلية: مجلدات/ملفات user‑controlled عبر SAF (Storage Access Framework)؛ الفحص خلفية (WorkManager).
- جلسات الاستماع: طويلة يومية، غالبًا ليلاً؛ Sleep Timer خط محوري في التجربة.
- الأوضاع الثلاثة إلزامية وتعمل فوريًا: Light (دافئ غير صارخ) / Dark (رمادي-مزرق وليس أسود مطلق) / AMOLED (أسود نقي #000000).
- الشاشات الإحدى عشرة (توجد فعلًا مبنية في الكود، مساراتها الحالية في `app/src/main/java/com/example/audiobook/presentation/*`):
  1. Home — ترحيب + Continue Listening + اختصارات أقسام المكتبة.
  2. Library — All Books بمصدر بيانات حقيقي، قسم Current/Finished/Favorites/Collections/Recently Added، بحث عربي بتطبيع، أقسام/فلترة/فرز، Grid/List toggle، Continue Listening card، مؤشر الملف المفقود (MISSING) على الغلاف.
  3. BookDetails — مركز إدارة الكتاب: Metadata، الغلاف، إدارة الإصدارات، الفصول، Bookmarks/Notes، التقدم، إحصائيات الكتاب.
  4. Player — الغلاف + Gradient ديناميكي، المتحكمات (Previous/−15s/Play−Pause/+15s/Next)، Timeline بمستوييه، زر Mark، شريط Speed/Sleep Timer/More (فقط المذكورة في الـSpec، بلا أي أزرار زائدة).
  5. Bookmarks — قائمة مستقلة، الضغط يقفز للـPlayer عند نفس اللحظة.
  6. Statistics — وقت الاستماع (يوم/أسبوع/شهر)، كتب/فصول مكتملة، Streak، متوسط السرعة.
  7. History — قائمة زمنية بجلسات الاستماع (اسم الكتاب، التاريخ، المدة).
  8. ScanReview (Review Matches) — مطابقات بثقة متوسطة/منخفضة + القرارات الثلاثة (نفس الإصدار / إصدار مختلف / ليس نفس الكتاب).
  9. Settings — تبديل الثيم فوري (Light/Dark/AMOLED)، مستوى الذكاء، افتراضيات Sleep Timer.
  10. LibraryRoots — المجلدات المضافة، تفعيل/تعطيل، Priority، حالة الفحص (IDLE/SCANNING/ERROR).
  11. DesignSystemShowcase — معرض عناصر نظام التصميم.
- التطبيق يُختبر آليًا فعليًا: بنية `androidTest` موجودة (SmokeTest)، ومعها طقم اختبارات JVM واسع (13+ ملفًا، تشغيل R8 = 130/130 أخضر في `R6_FINAL_REPORT.md`).

## Capabilities and Constraints

- [مؤكد بالكود] خطوات ذكية منفَّذة: `ScanRoot` (فحص/فهرسة، تكامل الإصدارات بثقة)، `SleepTimerController` (آلة حالات IDLE→RUNNING→WARNING→FADING→STOPPED، قواعد +15 دقيقة، نافذة 3 دقائق، حفظ موضع فورًا، تسجيل `ListeningSession`)، `StatisticsRules` (Streak وتسجيل اكتمال الفصل عند 90% بالضبط)، `EditionSignals`/`EditionMerge`/`ReviewMatches` (قرارات دمج/فصل مكتوبة في `EditionMatchDecision`).
- [مؤكد بالكود] الواجهة في وضعها الحالي: تعرض بيانات حقيقية عبر Room بعد إعادة هيكلة R1، لكنها تقع في أعطال بصرية موثقة (قسم "تشخيص فعلي من اللقطات المرفقة" في `ATHER_REDESIGN_IMPECCABLE_PROMPTS.md`): فراغ رأسي، روابط "نص عائم" شبيهة بـDebug، عدّادات مكررة، تسريب بيانات خام في "لا نتائج"، رقائق فلترة تتقطع خلف شريط التنقل، رقاقة بحرف واحد، أغلفة مربعات فارغة، فقاعة Autofill/Paste تتقاطع مع حقل البحث، وأنماط Material افتراضية في Home.
- [قرارات تأسيسية مؤكَّدة من المستخدم للمرحلة]: اسم التطبيق يصبح "أثير / Ather" مع استبدال كل ذكر "مكتبتك"؛ `applicationId` يبقى `com.example.audiobook` دون تغيير (قرار المستخدم).
- [مؤكد] لا توجد `strings.xml` بعد — كل النصوص مكتوبة داخل الـComposables؛ مرحلة إعادة الكتابة تنقلها لـstrings.xml منظمة.
- [مستنتج من المراجع، يُراجع عند توفر `AUDIOBOOK_APP_SPEC.md v2`]: تفاصيل MUST الدقيقة لكل فيتشر (مثل النسب والحدود الحرفية لقسم 13 والـPlayer)؛ الملف غير موجود في المستودع حاليًا، والمرجع المستعمل هو `AUDIOBOOK_REMEDIATION_PLAN.md` + `AUDIOBOOK_FRONTEND_EMULATOR_PROMPTS.md` + ملف إعادة التصميم نفسه.

## Brand Commitments

- الاسم العربي الأساسي الظاهر في كل الواجهة: **أثير**؛ اللاتيني للعلامة/Splash لاحقًا: **Ather**. استبدال أي "مكتبتك" ظاهر للمستخدم بالاسم أثير.
- النبرة: عربية فصيحة بسيطة، دافئة وواثقة، جمل قصيرة؛ بلا رسمية جافة (مثال: "أهلًا بك" وليس "مرحبًا بكم في تطبيقنا") وبلا عامية مبتذلة.
- الأوضاع الثلاثة (Light/Dark/AMOLED) إلزامية لكل الشاشات المرتبطة بالثيم بلا استثناء.
- ممنوع صراحة: التدرج البنفسجي-الأزرق الافتراضي، الخطوط الافتراضية العامة، البطاقات المتداخلة، النص الرمادي على خلفيات ملوّنة، الأيقونات داخل مربعات دائرية فوق كل عنوان.

## Evidence on Hand

- لقطات الحالة الحالية على المحاكي: `docs/screenshots/` (00_baseline … 16_section_recent + لقطات Library بالأوضاع الثلاثة وغيرها).
- تقرير تشغيل الاختبارات الكلية: `R6_FINAL_REPORT.md` (R7=124/124، R8=130/130 أخضر) — ومنه Addenda R7/R8 لحالة الثلاث نقاط الذكية.
- كود الواجهة كاملاً بمكانه الحالي: `app/src/main/java/com/example/audiobook/presentation/**` (ملفات الـ11 شاشة + ViewModels الخاصة بها).
- فجوة مكشوفة: `AUDIOBOOK_APP_SPEC.md v2` غير موجود في المستودع؛ يُراجع مرجعًا عند توفره — لا تُختلق تفاصيل MUST غائبة عنه.

## Product Principles

1. **بيانات حقيقية لا Placeholder**: كل شاشة تعرض بيانات من Room عبر ViewModel/Repository حقيقي؛ لا نصوص ثابتة داخل الـComposable كمحتوى.
2. **الذكاء قيمة فعلية لا شكل**: اكتشاف الإصدارات والمؤقت والإحصاءات تعمل بمنطق حقيقي قائم على إشارات وقواعد — لا demo IDs ولا أرقام ثابتة.
3. **راحة الجلسة الطويلة فوق كل لمعان**: تصميم لساعات استماع يومية غالبًا ليلًا — الهدوء البصري وراحة العين أهم من البهرجة.
4. **العربية RTL أولًا وأخيرًا**: أي قاعدة تصميم تتعارض مع RTL تُعدَّل لروحها فقط.
5. **الإثبات الصادق**: لا يُدَّعى اختبار أو جاهزية إلا بدليل قابل للتحقق (اختبار مكتوب + مخرجات، أو لقطة محاكي حقيقية، أو مراجعة كود مصرَّح بها صراحة).

## Accessibility & Inclusion

- واجهة عربية RTL كاملة (المحاذاة، اتجاه الانزلاق/الحركة، ترتيب قراءة الشاشة).
- `contentDescription` وصفي (وليس "زر") لكل عنصر تفاعلي بلا نص مرئي مرافق (Phase R5، مع اختبارات Semantics في `app/src/test/.../accessibility`).
- أهداف لمس ≥48×48dp ومسافات ≥8dp بينها؛ تخطيط يتحمّل تكبير الخط (sp/weight بدل px/ارتفاعات ثابتة).
- أوضاع إضاءة إلزامية مختبرة (Light/Dark/AMOLED) مع فحص التباين.