# تقرير إصلاح تراكب الملاحظة في المشغّل (Player Note Overlay)

**المكوّن:** `app/src/main/java/com/example/audiobook/presentation/player/PlayerScreen.kt`
**الالتزام (commit):** لا يوجد — كامل التغييرات غير مُودَعة على فرع `main`.
**تاريخ:** 2026-09-17

---

## أ) الملخّص التنفيذي

كانت الملاحظة عند عبورها أثناء التشغيل تظهر في بطاقة صغيرة **غير مقروءة**: خلفية شبه شفافة،
نص بلون التمييز على خلفية مصبوغة بالتمييز (تباين ضعيف)، مقتطعة عند ثلاثة أسطر مع زرّ إغلاق،
وبلا ترويسة أو ختم زمني. أُعيد بناء التراكب ليصبح **سطحًا زجاجيًا معتمًا**، أعلى الشاشة أسفل
ترويسة المشغّل مباشرةً، يعرض **النص الكامل بلا اقتطاع** مع ترويسة (أيقونة + «ملاحظة محفوظة» +
ختم زمني) وفاصل رقيق، ويظهر ~٦ ثوانٍ ثم يتلاشى تلقائيًا بلا أي تفاعل، مع نغمة تلميح قصيرة هادئة
تُشغَّل **مرّة واحدة فقط** عند كل عبور.

جميع الاختبارات (١٤٥) ناجحة، والبناء والتركيب على المحاكي ناجحان، والتحقق المرئي/السلوكي على
`emulator-5554` أكّد: عدم التغطية، النص الكامل، التلاشي التلقائي، استمرار التشغيل، وإعادة
الانطلاق عند العبور مجددًا. **نغمة التلميح لم تُتحقَّق سمعيًا** (لا التقاط صوت للمحاكي).

---

## ب) السبب الجذري (Root Cause)

قبل الإصلاح كان الأمر الفعّال في بطاقة الملاحظة:

1. **سطح غير مقروء.** استُخدم `fg.colors.popupSurface` وحده، وهو بلون أبيض/داكن بشفافية
   ~١٢٪ فقط (`0x1E`)؛ لا يوفّر خلفية كافية لفصل النص عن خلفية الغلاف المموّهة.
2. **تباين لوني معكوس.** كان نص الملاحظة بلون التمييز (`accent`) فوق سطح مصبوغ بالتمييز نفسه،
   فيقع المحظور: نص برتقالي على خلفية برتقالية.
3. **اقتطاع النص.** كان النص مرسومًا بـ `maxLines = 3` مع `TextOverflow.Ellipsis`، فلا تظهر
   الملاحظة كاملة.
4. **بنية ناقصة.** لم تكن هناك ترويسة ولا أيقونة ولا ختم زمني، بل زر إغلاق صريح (مخالف للمطلوب).
5. **تسلسل العبور.** منطق الكشف كان بسيطًا بلا صف انتظار ولا إعادة تسليح عند الرجوع، فملاحظتان
   متقاربتان قد تتكدّسان أو تُفوَّتان.

---

## ج) الإصلاح المطبَّق

### 1. سطح مقروء بأدوار ألوان صحيحة (بلا تصميم جديد)

- الخلفية صارت طبقتين مركّبتين: `fg.colors.scrim` ثم `fg.colors.popupSurface` فوقه، مع حدّ
  RoundedCornerShape(20.dp) وإطار `fg.colors.popupOutline` — كلها أدوار لونية موجودة مسبقًا.
- تعيين الأدوار:
  - نص المتن = `fg.colors.popupInk` (كامل العتامة).
  - الترويسة والختم الزمني = `fg.colors.popupSoft`.
  - الأيقونة = `fg.colors.accent`.
  - الإطار = `fg.colors.popupOutline`.
- `lerp` المتدرّج للون السطح يعتمد انعكاسًا صحيحًا بين الوضعين الفاتح والداكن (كما في النظام القائم).

### 2. الموضع: أعلى الشاشة أسفل الترويسة

- حُذف القياس السابق لموضع أزرار النقل (`transportTopPx`) لأنه أنتج تداخلًا فعليًا مع شريط النقل
  (أسفل جسم البطاقة ≈ `y=2030` مقابل أعلى شريط النقل ≈ `y=1958`).
- استُبدل بـ `headerBottomPx` المقيس عبر `Modifier.onGloballyPositioned { it.positionInRoot().y + it.size.height }`
  على ترويسة المشغّل، وحُسب `noteTopPadding` منه + `AppSpacing.xs`.
- لم يُعدَّل تصميم الغلاف ولا التدرّج ولا شريحة الفصل العلوية ولا الشريط الزمني ولا أزرار النقل.

### 3. منع الاقتطاع وعرض البنية الكاملة

- المتن: `bodyLarge` بلا `maxLines`/`Ellipsis`، مع `fillMaxWidth()`.
- ترويسة: صف يحوي أيقونة `Icons.AutoMirrored.Outlined.StickyNote2` + عنوان «ملاحظة محفوظة»
  (`player_note_overlay_header`) + ختم زمني عبر `formatNoteTimestamp()` (صيغة `h:mm:ss` عند
  تجاوز الساعة، وإلا `mm:ss`).
- `HorizontalDivider` رقيق بين الترويسة والمتن.
- عرص: `widthIn(min = 240.dp, max = 460.dp)` داخل `Box` موسّط أفقيًا.
- حُذف زر الإغلاق (`Icons.Outlined.Close`) والسلسلة `player_note_overlay_close`.

### 4. الظهور والتلاشي بلا تفاعل

- `AnimatedVisibility` مع `fadeIn(tween(220))` و`fadeOut(tween(320))`.
- مدّة العرض `NOTE_OVERLAY_VISIBLE_MS = 6_000L` ثم `NOTE_OVERLAY_GAP_MS = 350L` فاصل قبل التالي.

### 5. منطق العبور (مرة واحدة + إعادة تسليح + تسلسل)

```
LaunchedEffect(playback.positionMs, renderedTimeline.bookmarks) {
    notes = bookmarks.filter { label.isNotBlank() }
    current = playback.positionMs; previous = lastWatchPosMs
    if (current < previous - SEEK_BACK_THRESHOLD_MS)          // رجوع للخلف
        consumedNoteIds = consumedNoteIds.filter { id -> notes.any { it.id == id && it.positionMs > current } }
    if (current > previous) {
        crossed = notes.filter { id !in consumedNoteIds && positionMs in (previous, current] }.sortedBy { positionMs }
        consumedNoteIds += crossed.map { it.id }
        crossed.forEach { noteAlerts.trySend(NoteAlert(it.label, it.positionMs)) }
    }
    lastWatchPosMs = current
}
```

- **مرة واحدة:** كل ملاحظة تُضاف إلى `consumedNoteIds` فورًا فلا تتكرّر عند الثبات.
- **إعادة تسليح:** عند القفز للخلف أكثر من `SEEK_BACK_THRESHOLD_MS = 2_000L` تُحرَّر الملاحظات
  التي أصبحت أمام الموضع الحالي فقط.
- **جلسة جديدة:** `LaunchedEffect(editionId)` يصفّر `consumedNoteIds` و`lastWatchPosMs`.
- **تسلسل بلا تكديس:** قناة `Channel<NoteAlert>(UNLIMITED)` ومستهلك واحد يعيّن
  `activeNoteAlert` واحدًا في كل مرة، فتظهر الملاحظات **واحدة تلو الأخرى** مهما تقاربت.
- ملاحظة إصلاحية: عند تحرير معاينة ملاحظة يدويًا (`onPreviewNote`) تُرسَل إلى القناة نفسها، فلا
  تتصادم مع العرض التلقائي.

### 6. تمرير التوقيت

- وُسِّعت `onPreviewNote` من `(String) -> Unit` إلى `(String, Long) -> Unit` في السلسلة كاملة
  (`PlayerTimelineSection` → `SeekTrackZone` → `SeekMarkerTapLayer` → النداءات)، ليمرّر كل موضع
  `bookmark.positionMs` للختم الزمني.

---

## د) نغمة التلميح الصوتية

- `NoteCueTone` يستخدم `ToneGenerator` **الموجود أصلًا** (لا مكتبة جديدة).
- المسار `AudioManager.STREAM_MUSIC` → تحترم مستوى وسائط الجهاز.
- `ToneGenerator.TONE_PROP_PROMPT` بمستوى `NOTE_CUE_VOLUME = 22` ومدة `NOTE_CUE_DURATION_MS = 140`
  — قصيرة جدًا، بلا تكرار ولا إنذار ولا خفض صوت عنيف.
- تُنشأ كسولًا وتُحرَّر في `onDispose`؛ وأي فشل على الأجهزة التي لا توفّر `ToneGenerator`
  يُبتَلع صامتًا.
- **حالة التحقق: لم تُتحقَّق سمعيًا** (تعذّر التقاط صوت المحاكي) — تحقّق مراجعة كود فقط.

---

## هـ) نتائج اختبارات الانحدار

| السيناريو | النتيجة | الدليل |
|---|---|---|
| الاختبارات الوحدوية | **PASS** | `145` اختبارًا، `0` فشل، `0` خطأ، `0` متجاهَل (٣٧ ملف نتائج) عبر `:app:testDebugUnitTest` |
| البناء `assembleDebug` + التركيب | **PASS** | `BUILD SUCCESSFUL`، و`adb install -r` = Success على `emulator-5554` |
| قراءة النص الكامل بلا اقتطاع | **PASS** | بطاقة بعرض كامل؛ ملاحظة طويلة امتدّت سطرين `[105,566]–[975,719]` دون «…» |
| الموضع الصحيح أعلى الشاشة | **PASS** | الترويسة `[189,472][907,541]`، الختم `[105,487][189,527]`، المتن `[105,566][975,645]` |
| عدم تغطية الغلاف/الشريط الزمني/الشريحة | **PASS** | الغلاف يبدأ `y=761`، الشريط الزمني `y≥1958`، شريحة الفصل `y≤283` — لا تقاطع |
| التلاشي التلقائي خلال ~٦ ثوانٍ | **PASS** | تفريغ `uiautomator dump` بعد ٥ ثوانٍ = «اختفى التراكب» |
| استمرار التشغيل دون إيقاف | **PASS** | زر النقل أظهر `إيقاف` أثناء ظهور التراكب |
| إعادة الانطلاق عند العبور مجددًا | **PASS** | بعد الرجوع أسفل الملاحظة (22:16) ثم التشغيل، ظهرت الترويسة والختم `22:30` والمتن كاملًا |
| العبور مرة واحدة دون تكرار عند الثبات | **PASS** | `consumedNoteIds` + اقتصار الشرط على `(previous, current]` |
| ملاحظة ثانية بنصّها الكامل | **PASS** | ملاحظة مُدخَلة عند 22:34 عرضت متنها الكامل `[105,566][975,719]` وختمها `22:34` |
| التسلسل التام لملاحظتين متقاربتين (بلا تكديس) | **PASS جزئي / بحكم التصميم** | مستهلك واحد + `activeNoteAlert` واحد؛ لوحظت الثانية تُرسَم بنص كامل، لكن لم يُلتقط تعاقبُهما لحظة بلحظة على الجهاز |
| نغمة التلميح مسموعة | **NOT TESTED** | تعذّر التقاط الصوت على المحاكي |
| تباين الألوان بصريًا | **NOT TESTED (بصريًا)** | تعذّر على الأداة قراءة لقطات الشاشة؛ التحقق عبر تعيين الأدوار في الكود + حدود العناصر |

---

## و) الملفات المعدّلة والقيود

### الملفات

- `app/src/main/java/com/example/audiobook/presentation/player/PlayerScreen.kt`
  - الحالة: `noteVisible` (س183)، `activeNoteAlert` (س184)، `headerBottomPx` (س188).
  - `noteTopPadding` (س232–233) أسفل الترويسة.
  - منطق العبور: القناة (س303)، إعادة الضبط على `editionId` (س314)، الكشف/إعادة التسليح (س320–339)،
    المستهلك التسلسلي (س342–351).
  - القياس: `headerBottomPx = it.positionInRoot().y + it.size.height` (س390).
  - النداء: `NoteOverlay(visible, alert, headerTitle, fg)` (س639–640).
  - `onPreviewNote = { text, pos -> noteAlerts.trySend(NoteAlert(text, pos)) }` (س470–471).
  - `NoteAlert` (س657)، الثوابت (س660/663/666)، `formatNoteTimestamp` (س802).
  - تواقيع `onPreviewNote: (String, Long) -> Unit` (س941، 1002، 1113) والتمرير (س971، 1043، 1117).
  - إعادة كتابة `NoteOverlay` (س2173–2233) و`NoteCueTone` (س2236–2250) والثوابت (س2253، 2256).
  - الاستيرادات: `widthIn`، `HorizontalDivider`، `kotlinx.coroutines.channels.Channel`،
    `androidx.compose.ui.layout.positionInRoot`،
    `androidx.compose.material.icons.automirrored.outlined.StickyNote2`؛ وحُذف `outlined.Close`.
- `app/src/main/res/values/strings.xml`
  - `player_note_overlay_header` = «ملاحظة محفوظة» (س222) بدلًا من
    `player_note_overlay_cue` و`player_note_overlay_close` (محذوفتان).

> ملاحظة: بقية الملفات غير المُودَعة في `git status` تخصّ تمريرة «توحيد الإعدادات» السابقة
> (`AppSettings` وغيرها) وليست جزءًا من إصلاح تراكب الملاحظة.

### القيود

1. **النغمة الصوتية:** لم تُتحقَّق سمعيًا؛ اعتماد على مراجعة الكود فقط.
2. **العرض التسلسلي:** لم يُلتقط تعاقب ملاحظتين متقاربتين لحظةً بلحظة (أُكِّد بحكم كون
   `activeNoteAlert` واحدًا + مستهلك واحد، ولوحظت الملاحظة الثانية تُرسَم بنصّها الكامل).
3. **التحقق البصري:** لون/تباين البطاقة لم يُفحَص بالعين من قِبل الأداة (تعذّر قراءة لقطات
   الشاشة)؛ استُند إلى تعيين أدوار الألوان في الكود + حدود العناصر من `uiautomator`.
4. **تغطية الاختبارات:** لا يوجد اختبار آلي مباشر لسلوك التراكب (`PlayerScreenAccessibilityTest`
   لا يغطّيه)؛ الاختبارات الـ١٤٥ لم تتأثّر لأن التغيير داخلي بحت.
5. تنظيف لاحق: أُزيلت الملاحظة الاختبارية المُدخَلة عند 22:34 من قاعدة بيانات المحاكي، وبقيت
   الملاحظتان الأصليتان فقط.
