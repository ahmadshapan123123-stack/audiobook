# برومبت: تحقّق فعلي من عمل التطبيق + بناء APK — وليس مجرد Compile

اقرأ قبل أي سطر كود، بالكامل وبالترتيب:
1. `plan.md` (المرجع الوحيد الكامل للمنتج — Source of Truth)
2. `AUDIOBOOK_REMEDIATION_PLAN.md` (خطة الإصلاح، وتحديدًا **Phase R0**)
3. `R6_FINAL_REPORT.md` (آخر تقرير موجود في المستودع)

## المشكلة التي يجب أن تحلّها هذه المرة تحديدًا

آخر تسليم أنتجته (أو وكيل سابق) كان تقريره الكامل هو:
```
gradlew.bat assembleDebug --no-daemon → BUILD SUCCESSFUL
APK موجود، حجمه 22.91MB
الاستنتاج: "البناء نجح. الملف جاهز للتثبيت."
```
هذا **غير كافٍ ومضلِّل**. عند تثبيت هذا الـ APK فعليًا على جهاز، ظهر بلا صفحة رئيسية، بلا قائمة تنقل، بلا أي شيء يعمل. السبب الموثّق ذاتيًا في `R6_FINAL_REPORT.md`:

> `app/src/androidTest` **لا وجود له إطلاقًا**. كل الاختبارات JVM unit tests (Robolectric). لا يوجد جهاز/محاكي في هذه البيئة.

يعني: **لم يُشغَّل التطبيق فعليًا ولو مرة واحدة على جهاز/محاكي حقيقي منذ بداية المشروع.** كل "الاختبارات الناجحة" (130 اختبار) تختبر منطقًا معزولًا، لا تُثبت أن `Activity.onCreate()` يعمل، ولا أن Hilt يُهيّأ بنجاح وقت التشغيل الحقيقي، ولا أن الشاشة تُرسَم فعليًا، ولا أن فحص الملفات يجد كتبًا حقيقية.

**"BUILD SUCCESSFUL" ≠ "التطبيق يعمل".** ممنوع الخلط بين الاثنين في أي تقرير تكتبه.

## القاعدة الإلزامية الوحيدة لهذه المهمة

**لا يُقبل أي ادّعاء بلا دليل تشغيل فعلي.** لكل نقطة أدناه، الدليل المقبول الوحيد هو أحد:
- **تشغيل فعلي على محاكي/جهاز حقيقي** (لقطة logcat كاملة + خطوة تثبيت + خطوة فتح فعلية)
- **اختبار Instrumented حقيقي** داخل `app/src/androidTest` نُفِّذ فعلًا (وليس Robolectric بديلاً عنه)
- إن تعذّر أي منهما فعليًا في بيئتك: **صرّح بذلك صراحة** ("لا يوجد محاكي متاح، لم أتمكن من التحقق فعليًا من X") بدل أي صياغة عامة مثل "تم التأكد" أو "يعمل بشكل ممتاز".

إن لم يوجد محاكي Android في بيئتك حاليًا: **ثبّت واحدًا وشغّله فعلًا** (عبر `avdmanager` + `emulator` من Android SDK command-line tools، أو أي طريقة متاحة) قبل الاستمرار. لا تنتقل لخطوة "البناء النهائي" بدون محاكي يعمل فعليًا وتقدر تثبّت عليه وتراقبه.

---

## الخطوات المطلوبة بالترتيب

### 1. افحص وأصلح أي شذوذ في المشروع أولًا
- في `app/src/main/AndroidManifest.xml` يوجد هذا العنصر:
  ```xml
  <activity
      android:name="androidx.activity.ComponentActivity"
      android:exported="false"
      android:theme="@style/Theme.Audiobook"
      tools:replace="android:exported,android:theme" />
  ```
  هذا النمط عادة يتسرّب من `androidx.compose.ui:ui-test-manifest` (وهي مُضافة في `build.gradle.kts` كـ `testImplementation` بدل أن تكون محصورة في `androidTest`). **افهم سبب وجوده فعليًا قبل حذفه أو الإبقاء عليه** — لا تحذفه فقط لأن التقرير طلب ذلك، ولا تتركه لمجرد أن البناء ينجح معه. وثّق التشخيص الحقيقي.
- تأكّد أن التبعية `androidx.compose.ui:ui-test-manifest` منقولة إلى `androidTestImplementation` إن كانت المشكلة من هناك، وليست `testImplementation`.

### 2. أنجز Phase R0 فعليًا (لم تُنجَز أبدًا رغم أنها كانت الشرط الأول)
- أضف موديول `app/src/androidTest` حقيقي مع `androidx.test.ext:junit`, `androidx.test.espresso:espresso-core`, `androidx.compose.ui:ui-test-junit4` (كـ `androidTestImplementation`).
- اكتب Smoke Test حقيقي: `launchActivity<MainActivity>()` ثم تحقّق أن `LibraryScreen` تُعرض فعليًا بدون Crash، على محاكي حقيقي تشغّله أنت.
- شغّله فعليًا عبر `./gradlew connectedDebugAndroidTest` وأرفق المخرجات الحرفية.

### 3. تحقّق أن التطبيق يبحث فعليًا عن الملفات ويسحب كتبًا حقيقية من الجهاز
هذا جوهر شكوى المستخدم ويجب إثباته عمليًا لا نظريًا:
- على المحاكي، ضع ملفات صوتية حقيقية (MP3/M4A على الأقل) داخل مجلد تجريبي واضح البنية (مثال: `AudiobookTest/الكاتب/الكتاب/ملفات.mp3`)، عبر `adb push`.
- شغّل التطبيق، امنح صلاحية SAF لهذا المجلد (تلقائيًا عبر UI Automator/Espresso إن أمكن، أو وثّق الخطوة اليدوية إن تعذّر أتمتتها وقل ذلك صراحة).
- شغّل الفحص (Scan) من شاشة LibraryRoots، وانتظر انتهاءه فعليًا.
- التقط لقطة شاشة/تسجيل يُثبت أن **الكتاب الحقيقي الذي وضعته أنت ظهر في شاشة المكتبة** بعنوانه واسم مؤلفه المستخرَج من مسار الملف الفعلي — وليس بيانات `EnsureDemoEdition` التجريبية. تأكّد أن `EnsureDemoEdition` لا يُشغَّل في هذا المسار (هي خلف `BuildConfig.DEBUG` حاليًا، وهذا قد يُزيّف الاختبار — عطّلها مؤقتًا أثناء هذا التحقق تحديدًا لتضمن أن ما تراه هو نتيجة فحص حقيقي فقط).
- اضغط على الكتاب → افتح تفاصيله → شغّل الاستماع → تأكد أن الصوت الفعلي يُشغَّل (أو على الأقل أن PlaybackService يبدأ بلا Crash ويُظهر تحكّمًا حقيقيًا بالتقدّم).

### 4. اختبر رحلة المستخدم الكاملة فعليًا على المحاكي، خطوة بخطوة
ثبّت التطبيق → افتحه → تحقق أن **الشاشة الرئيسية تظهر فعليًا** (لا شاشة بيضاء/سوداء/Crash) → أضف مجلد مكتبة → شغّل فحصًا → افتح كتابًا حقيقيًا → افتح المشغّل → أضف Bookmark → ارجع → افتح الإحصائيات → افتح السجل (History) → افتح شاشة مراجعة المطابقات (Review Matches) → افتح الإعدادات وغيّر مستوى الذكاء → ارجع للمكتبة.
لكل خطوة: التقط logcat (`adb logcat *:E AndroidRuntime:E` أثناء التنقل) وتأكد من **صفر Crash وصفر ANR** طوال الرحلة. أي Crash يظهر: أصلحه فعليًا في الكود (لا تُخفِه)، ثم أعد الاختبار من الصفر.

### 5. لا تنتقل لأي بند آخر (منطق الفحص الذكي، الخ) قبل ما فوق
الأولوية المطلقة الآن: **إثبات أن التطبيق يُقلع، يُبحر بين شاشاته، ويسحب كتبًا حقيقية من الجهاز، بلا Crash — بدليل تشغيل فعلي.** أي إصلاح آخر (خوارزميات الثقة، المؤقت الذكي...) يأتي بعد هذا فقط.

### 6. بناء الـ APK النهائي
فقط بعد نجاح كل ما سبق بدليل حقيقي:
```
gradlew.bat assembleDebug --no-daemon
```
ثم `adb install -r` للنسخة النهائية على نفس المحاكي، وتأكيد أخير أنها تفتح وتعمل (نفس فحص logcat).

---

## صيغة التقرير المطلوبة (إلزامية)

لكل بند أعلاه، جدول بعمودين بالضبط كما في `R6_FINAL_REPORT.md`: **"الحالة الفعلية"** و **"نوع الدليل"** (تشغيل فعلي على محاكي / instrumented حقيقي / مراجعة كود / غير مُنجز). ممنوع أي عبارة "تم التأكد" أو "البناء نجح = جاهز" بدون دليل مرفق مباشرة بجانبها. إن كان أي بند لم يُختبر فعليًا لأي سبب (بيئة، وقت، صلاحيات)، اكتب "غير مُنجز" صراحة بدل تجاوزه أو التلميح بأنه تم.

انتهِ فقط عندما يكون لديك: (أ) محاكي شغّال فعليًا، (ب) logcat نظيف طوال رحلة مستخدم كاملة، (ج) كتاب حقيقي واحد على الأقل ظهر في المكتبة نتيجة فحص فعلي لا بيانات تجريبية، (د) APK نهائي مثبّت وعامل على نفس المحاكي، (هـ) تقرير صادق بالجدول أعلاه.
