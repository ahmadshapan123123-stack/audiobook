# إعادة تصميم الواجهة الأمامية بالكامل — مشروع "أثير" (Ather) عبر Impeccable + OpenCode

هذا الملف **مكمّل** لـ`AUDIOBOOK_APP_SPEC.md v2` و`AUDIOBOOK_REMEDIATION_PLAN.md` و`AUDIOBOOK_FRONTEND_EMULATOR_PROMPTS.md`. الهدف هنا: إعادة بناء الواجهة الأمامية بالكامل بهوية بصرية ونصية قوية تحت اسم **أثير (Ather)**، باستخدام مهارة [Impeccable](https://impeccable.style) عبر **OpenCode**، مع إصلاح كل الأعطال المرصودة فعليًا من لقطات الشاشة الحالية.

**[MUST] لا يُلغي هذا الملف أي قرار MUST في `AUDIOBOOK_APP_SPEC.md v2`**: RTL الكامل، الأوضاع الثلاثة (Light/Dark/AMOLED)، التطبيع العربي للبحث، عناصر التحكم المذكورة حرفيًا في شاشة الـPlayer — كل ده يبقى إلزاميًا. Impeccable هيحسّن **كيفية** تنفيذ هذه المتطلبات بصريًا، مش هيغيّرها.

---

## تشخيص فعلي من اللقطات المرفقة (مرجع أعطال إلزامي الإصلاح)

كل نقطة هنا لازم تُذكر صراحة كـ"تم إصلاحها" في أي تقرير تنفيذ لاحق، بدليل لقطة شاشة:

1. **فراغ رأسي ضخم فارغ** بين روابط "السجل/الإحصائيات/مراجعة المطابقات" وبداية المحتوى الفعلي في شاشة المكتبة.
2. الروابط الثلاثة دي **نص عائم بلا هوية بصرية** — تبدو كعناصر Debug مش تنقلًا مصممًا (مفروض تختفي كـ"نص" وتتحول لعناصر تنقل حقيقية أو Overflow Menu منظّم).
3. **تكرار عدّادات متضاربة الصياغة**: "كتاب في المكتبة 10" في مكان و"كتب 10" في مكان آخر من نفس الشاشة — يجب توحيدها في مصدر نص واحد.
4. عند البحث بدون نتائج: **تسريب بيانات خام غير منسّقة** (اسم مؤلف مكرر + شريط تقدم رفيع بلا بطاقة) بدل حالة "لا نتائج" مصمَّمة بوضوح.
5. **رقائق الفلترة (Chips) تتقطع فعليًا خلف شريط التنقل السفلي** — تراكب حقيقي يمنع الوصول لبعض الخيارات.
6. رقاقة فلتر بنص **مقصوص لحرف واحد** ("ا").
7. أغلفة الكتب **مربعات لون صلب فارغة تمامًا** — بلا حرف أول، بلا نمط، بلا أي هوية.
8. **فقاعة Autofill/Paste من النظام** تتقاطع بصريًا مع حقل البحث.
9. شاشة Home هيكليًا سليمة لكن **نمطية Material افتراضية بحتة** (بطاقات رمادية موحدة، أزرار Outline عامة، تدرج بنفسجي-أزرق كلاسيكي) — من الأنماط التي يصنّفها Impeccable كـ"AI slop".

---

## القرار الهوياتي: أثير / Ather

- **الاسم العربي (الأساسي، ويظهر في كل الواجهة)**: أثير
- **الاسم اللاتيني (للعلامة التجارية/Splash/متاجر التطبيقات لاحقًا)**: Ather
- استبدال أي ذكر لـ"مكتبتك" في أي نص ظاهر للمستخدم بالكامل.
- **قرار افتراضي** (وضّح غيره إن أردت): الإبقاء على `applicationId` الحالي في `build.gradle.kts` كما هو دون تغيير في هذه المرحلة — التغيير هنا في التسمية الظاهرة والهوية البصرية والمحتوى، وليس في مُعرّف الحزمة التقني (تغيير applicationId قرار أكبر يؤثر على النشر لاحقًا، اتركه لمرحلة منفصلة إن رغبت).

---

## بيئتك: OpenCode

Impeccable يدعم OpenCode رسميًا (`dist/opencode/.opencode`). التثبيت من جذر المشروع:

```bash
npx impeccable install
```

هذا الأمر بيكتشف OpenCode تلقائيًا ويضع ملفات الـSkill في `.opencode/` بالمشروع. لو حبيت التثبيت اليدوي المباشر بدل الاكتشاف التلقائي:

```bash
# داخل جذر مشروع audiobook-main
npx impeccable install --providers=opencode
```

بعدها أعد تشغيل/إعادة تحميل OpenCode داخل الترمينال، وتأكد بكتابة `/impeccable` إنه بيطلع قائمة الأوامر (audit, critique, craft, polish, colorize, typeset, layout, distill, animate...).

---

## Prompt 1 — تثبيت Impeccable + تأسيس هوية "أثير" (DESIGN.md / PRODUCT.md)

```
أنت تعمل داخل OpenCode على مشروع audiobook-main (Kotlin + Jetpack Compose). المرجع الكامل للمنتج هو AUDIOBOOK_APP_SPEC.md v2 وAUDIOBOOK_REMEDIATION_PLAN.md وAUDIOBOOK_FRONTEND_EMULATOR_PROMPTS.md الموجودين في جذر المشروع — اقرأهم أولاً.

المطلوب في هذه الخطوة فقط:

1. ثبّت Impeccable في المشروع: شغّل `npx impeccable install` من جذر المشروع، وتأكد من ظهور مجلد `.opencode/` بملفات الـSkill. أعد تحميل الجلسة وتأكد أن `/impeccable` يعمل ويعرض قائمة الأوامر.
2. شغّل `/impeccable init` وأجب على أي أسئلة تهيئة يطرحها الأداة.
3. أنشئ ملف `DESIGN.md` في جذر المشروع يوثّق هوية "أثير" بوضوح، ليقرأه Impeccable في كل أمر لاحق ولا ينحرف نحو أنماطه الافتراضية:
   - اسم التطبيق: أثير (عربي) / Ather (لاتيني).
   - طبيعة المنتج: تطبيق استماع لكتب صوتية عربية، تجربة استخدام طويلة يومية (ساعات، غالبًا ليلًا)، لذلك الهدوء البصري وراحة العين أهم من "البهرجة".
   - لغة الواجهة: عربية RTL كاملة أولًا وأخيرًا — أي مبدأ تصميم من Impeccable يتعارض مع RTL (مثل اتجاه الأيقونات، محاذاة النصوص، اتجاه الحركة/الانزلاق) يُعدَّل ليطابق RTL، لا يُطبَّق حرفيًا كما هو في أمثلة الأداة الخاصة بـLTR.
   - الأوضاع الإلزامية الثلاثة: Light (دافئ غير صارخ) / Dark (رمادي-مزرق وليس أسود مطلق) / AMOLED (أسود نقي #000000) — كما هو مفصّل في قسم 13 من AUDIOBOOK_APP_SPEC.md v2. Impeccable يجب أن يبني نظام ألوان يخدم الثلاثة أوضاع دي بالتحديد، لا نظامًا واحدًا فقط.
   - ممنوع صراحة: التدرج البنفسجي-الأزرق الافتراضي، الخطوط الافتراضية العامة، البطاقات المتداخلة داخل بعضها، النص الرمادي على خلفيات ملوّنة، الأيقونات الموضوعة داخل مربعات دائرية الحواف فوق كل عنوان (كل دول من نفس "الـAI slop" اللي التطبيق الحالي واقع فيه بالفعل حسب اللقطات المرفقة).
   - نبرة المحتوى (Voice): عربية فصيحة بسيطة، دافئة وواثقة، جمل قصيرة، بدون رسمية جافة وبدون عامية مبتذلة. مثال الطابع المطلوب: "أهلًا بك" وليس "مرحبًا بكم في تطبيقنا"؛ "لم نجد نتائج" وليس "عذرًا، لم يتم العثور على أي نتائج مطابقة لاستعلامك".
4. أنشئ ملف `PRODUCT.md` يلخّص الشاشات الإحدى عشرة وفيتشرزها الأساسية (من قسم "البنية المعمارية" في الـSpec) ليكون مرجع سياق ثابت لكل أوامر Impeccable اللاحقة.

توقف بعد كل نقطة واعرض محتوى DESIGN.md وPRODUCT.md كاملين للمراجعة قبل الاعتماد عليهما في أي خطوة تالية — هذان الملفان سيوجّهان كل قرار تصميمي بعد ذلك، فلازم مراجعتهما بعناية أولًا.
```

---

## Prompt 2 — تدقيق Impeccable على الوضع الحالي (قبل إعادة البناء)

```
بعد اعتماد DESIGN.md وPRODUCT.md، شغّل تدقيقًا كاملاً على الحالة الحالية للواجهة قبل تغيير أي كود:

1. شغّل `/impeccable audit` و`/impeccable critique` على كل شاشة من الشاشات الموجودة فعليًا حاليًا (Library, Home, BookDetails, Player إلخ حسب ما هو مبني من AUDIOBOOK_FRONTEND_EMULATOR_PROMPTS.md).
2. اجمع مخرجات الأداة مع الأعطال المرصودة يدويًا في قسم "تشخيص فعلي من اللقطات المرفقة" أعلاه، في قائمة واحدة موحدة (Issue List) بلا تكرار، كل عطل برقم مرجعي.
3. صنّف كل عطل: هل هو "بنية/تخطيط" (Layout bug مثل التراكب خلف شريط التنقل والفراغ الرأسي)، أم "نظام تصميم" (ألوان/خطوط/تدرجات نمطية)، أم "محتوى/نصوص" (تكرار عدادات، نص مقصوص، تسريب بيانات خام)؟

توقف واعرض القائمة الموحدة الكاملة مصنّفة — هذه القائمة هي معيار القبول (Acceptance Criteria) لكل البرومبتات التالية: أي عطل فيها غير مُصلَح صراحة لاحقًا، تقرير الإغلاق النهائي (Prompt 8) يعتبره فشلاً.
```

---

## Prompt 3 — إعادة التسمية الكاملة إلى "أثير / Ather"

```
بناءً على القرار الهوياتي في هذا الملف:

1. ابحث في كامل المشروع (strings.xml، كل Composable، AndroidManifest، أي تعليقات ظاهرة للمستخدم عبر الـUI، عنوان التطبيق في شريط النظام) عن أي ذكر لـ"مكتبتك" أو أي اسم مؤقت آخر، واستبدله بـ"أثير".
2. حدّث `android:label` في AndroidManifest.xml والـ`app_name` في strings.xml ليكون "أثير".
3. أضف Splash/شاشة ترحيب (لو غير موجودة) تعرض اسم "أثير" بالعربي مع "Ather" كعلامة لاتينية مصغّرة تحته (نمط بصري شائع للعلامات ثنائية اللغة) — استخدم `/impeccable craft` لتصميمها بما يتماشى مع DESIGN.md.
4. لا تغيّر `applicationId` في build.gradle.kts في هذه الخطوة (القرار الافتراضي أعلاه)، إلا لو طلبت غير ذلك صراحة.
5. راجع أيقونة التطبيق (ic_launcher) — لو كانت لا تزال Placeholder افتراضي من Android Studio، صمّم أيقونة بسيطة تعكس هوية "أثير" (رمز مرتبط بالصوت/الاستماع/الكتاب، بألوان نظام التصميم الجديد لاحقًا من Prompt 5 — يمكن تأجيل الأيقونة النهائية لبعد اعتماد الألوان، ووضع نسخة أولية الآن).

توقف واعرض كل الملفات التي تم تعديلها + Screenshot فعلي من المحاكي لشريط العنوان/الـSplash يظهر فيه "أثير" بوضوح.
```

---

## Prompt 4 — إعادة كتابة كل المحتوى والنصوص (UX Writing)

```
استخدم مرجع "UX Writing" ضمن Impeccable (من مراجع التصميم السبعة التي يحمّلها) بالتوازي مع نبرة "أثير" الموثّقة في DESIGN.md:

1. راجع كل نص ظاهر في الواجهة حاليًا (عناوين، أزرار، Placeholders، رسائل الحالة الفارغة، Toasts، تسميات الأقسام والفلاتر) وأعد كتابته بالكامل بنفس المعنى الوظيفي لكنه بصياغة أقوى وأكثر اتساقًا:
   - وحّد صياغة عدّادات الكتب (المشكلة رقم 3 في قائمة الأعطال): مصدر نص واحد يُستخدم في كل مكان بدل "كتاب في المكتبة 10" و"كتب 10" كصياغتين مختلفتين لنفس الرقم.
   - صمّم حالة "لا نتائج بحث" نصيًا وبصريًا بدل تسريب البيانات الخام الحالي (المشكلة رقم 4): رسالة واضحة + ربما اقتراح لتصحيح البحث.
   - أعد صياغة تسميات الفلاتر/الأقسام (كل الكتب، قيد الاستماع، مكتملة، المفضلة...) بحيث تكون مختصرة ومتسقة الطول (لتفادي مشكلة القص كما في رقاقة "ا" - المشكلة رقم 6).
2. لا تخترع فيتشرات جديدة أو تغيّر وظيفة أي شاشة — هذه إعادة صياغة نصية وليست تغييرًا وظيفيًا.
3. اجمع كل النصوص الجديدة في ملفات `strings.xml` منظمة (وليست نصوصًا مكتوبة داخل الـComposable مباشرة) تسهيلًا للمراجعة والترجمة المستقبلية.

توقف واعرض جدول "قبل → بعد" لكل نص جوهري تم تغييره، للمراجعة قبل الاعتماد.
```

---

## Prompt 5 — نظام التصميم الجديد بالكامل (Design Tokens) للأوضاع الثلاثة

```
استخدم `/impeccable shape` ثم `/impeccable craft` لبناء نظام تصميم جديد بالكامل من الصفر يحل محل الألوان/الأنماط النمطية الحالية، ويخدم متطلبات قسم 13 من AUDIOBOOK_APP_SPEC.md v2 حرفيًا:

1. لوحة ألوان مخصصة كاملة لثلاثة أوضاع (Light/Dark/AMOLED) — بدون التدرج البنفسجي-الأزرق الافتراضي الحالي (مشكلة رقم 9)، لون أساسي وألوان ثانوية لها هوية مميزة لـ"أثير" تحديدًا.
2. نظام Typography يدعم العربية RTL بشكل كامل وصحيح (لا خط افتراضي عام)، مع تدرّج أحجام واضح.
3. مقياس مسافات ثابت (4/8dp) يُطبَّق للتخلص نهائيًا من الفراغات العشوائية (مشكلة رقم 1) والتراكبات خلف شريط التنقل (مشكلة رقم 5) — كل شاشة يجب أن تحترم `safe area`/`WindowInsets` فعليًا حول شريط التنقل السفلي.
4. مكوّنات Compose أساسية معاد تصميمها بهذا النظام (أزرار، بطاقات كتب، Chips، حقول بحث) — حقل البحث تحديدًا يجب أن يُهيَّأ لمنع فقاعة Autofill/Paste من النظام من التداخل بصريًا (مشكلة رقم 8، عبر `KeyboardOptions`/`autofillType` المناسب).
5. صمّم غلاف بديل (Placeholder Cover) حقيقي لكل كتاب بلا صورة فعلية: الحرف الأول من العنوان + نمط/لون مشتق من هوية أثير، بدل المربع الصلب الفارغ الحالي (مشكلة رقم 7).
6. اختبر النظام على شاشة واحدة بسيطة في الأوضاع الثلاثة وخذ لقطة فعلية لكل وضع، وتأكد بصريًا من التباين وعدم وجود ألوان صارخة، قبل تعميم النظام على كل الشاشات.

توقف واعرض اللقطات الثلاث + ملف الـTokens (الألوان/الخطوط/المسافات) قبل الانتقال لإعادة بناء الشاشات فعليًا.
```

---

## Prompt 6 — إعادة تنفيذ كل شاشة (Craft → Polish → Audit) شاشة بشاشة

```
الآن أعد بناء كل شاشة من الشاشات الإحدى عشرة (Home, Library بكل أقسامها, BookDetails, Player, Bookmarks, Statistics, History, ScanReview, Settings, LibraryRoots) باستخدام نظام التصميم الجديد من Prompt 5، بالتسلسل التالي لكل شاشة على حدة:

لكل شاشة:
1. `/impeccable craft <اسم الشاشة>` لإعادة بنائها بنظام التصميم الجديد.
2. أصلح صراحة أي عطل من "قائمة الأعطال الموحدة" (Prompt 2) يخص هذه الشاشة تحديدًا.
3. `/impeccable polish <اسم الشاشة>` كخطوة تلميع أخيرة.
4. `/impeccable audit <اسم الشاشة>` وتأكد من عدم وجود تحذيرات متبقية.
5. شغّل الشاشة فعليًا على المحاكي، خذ لقطة شاشة حقيقية، واحفظها في docs/screenshots/ather/ باسم واضح.
6. للشاشات المرتبطة بالثيم (Library, BookDetails, Player, Settings, Home): كرر التقاط اللقطة في الأوضاع الثلاثة.

ابدأ بشاشة المكتبة (Library) تحديدًا لأنها تحتوي أكبر عدد من الأعطال المرصودة (7 من أصل 9)، ثم Home، ثم باقي الشاشات بأي ترتيب منطقي.

توقف بعد كل شاشة منفردة واعرض لقطاتها الفعلية قبل الانتقال للشاشة التالية — لا تبنِ كل الشاشات دفعة واحدة ثم تعرض النتيجة في الآخر.
```

---

## Prompt 7 — التأكد أن كل الفيتشرز الموجودة ظاهرة فعليًا في الواجهة

```
بعد إعادة بناء كل الشاشات بصريًا، هذه خطوة مطابقة صريحة تمنع اختفاء أي فيتشر أثناء إعادة التصميم:

1. استخرج من AUDIOBOOK_APP_SPEC.md v2 قائمة كاملة بكل فيتشر إلزامي (MUST) لكل شاشة (مثال: من قسم 5 — Grid/List toggle, Sort, Filter, Continue Listening card, Search بتطبيع عربي؛ من قسم 10 — كل أزرار التحكم المذكورة حرفيًا في الـPlayer وليس أقل ولا أكثر؛ من قسم 12 — عناصر Sleep Timer الظاهرة؛ إلخ).
2. ابنِ جدول مطابقة: **الفيتشر → الشاشة → العنصر البصري الذي يمثله فعليًا في الكود الحالي بعد إعادة التصميم**. أي فيتشر بلا عنصر بصري مطابق واضح = فجوة يجب سدّها الآن قبل الإغلاق.
3. تأكد تحديدًا أن إعادة التصميم لم "تبسّط" أو تحذف أي فيتشر بحجة التنظيف البصري (خطر شائع عند تطبيق Impeccable بقوة: حذف عناصر لتقليل الازدحام البصري قد يحذف فيتشرًا مطلوبًا فعليًا وليس مجرد ضوضاء بصرية).

توقف واعرض الجدول الكامل — أي صف فيه فجوة، أصلحه فورًا قبل الانتقال لـPrompt 8.
```

---

## Prompt 8 — جولة تدقيق وتحقق بصري نهائية

```
1. شغّل `/impeccable audit` على المشروع كاملاً (وليس شاشة بشاشة) كفحص أخير شامل، وأرفق مخرجاته الحرفية.
2. أعد تشغيل جولة End-to-End كاملة على المحاكي (نفس مسار Prompt 8 في AUDIOBOOK_FRONTEND_EMULATOR_PROMPTS.md) وسجّل فيديو جديد يوثّق التصميم الجديد بعد إعادة البناء.
3. راجع "قائمة الأعطال الموحدة" من Prompt 2 عطلاً عطلاً، وأثبت لكل واحد منها أنه أُصلح فعليًا بلقطة شاشة "قبل" (من اللقطات الأصلية المرفقة) و"بعد" (من التصميم الجديد) جنبًا إلى جنب.
4. اكتب تقريرًا ختاميًا: اسم التطبيق الآن "أثير" في كل مكان ✓/✗، كل الأعطال التسعة مُصلحة ✓/✗ (كل واحد على حدة)، كل الفيتشرز ظاهرة حسب جدول Prompt 7 ✓/✗، الأوضاع الثلاثة سليمة بصريًا على كل شاشة رئيسية ✓/✗.

لا تُغلق هذه المرحلة بدون هذا التقرير الكامل مع اللقطات المرفقة فعليًا.
```

---

## ملاحظة أخيرة: التوازن بين Impeccable ومتطلبات الـSpec

Impeccable ممتاز في كسر الأنماط النمطية (والتطبيق الحالي واقع في أغلبها فعلاً كما ظهر باللقطات)، لكن مراجعه مبنية أساسًا على تجارب ويب LTR. أهم قرار مسؤول أنت (أو من ينفّذ) لازم تاخده باستمرار عبر كل البرومبتات دي: **أي اقتراح تصميمي من الأداة يتعارض مع RTL العربي أو مع أي MUST في قسم 13 من الـSpec الأصلي، تُرفض حرفيته وتُطبَّق روحه فقط** (مثال: مبدأ "تجنّب الأيقونات في مربعات دائرية فوق العناوين" مبدأ عام صحيح بصرف النظر عن الاتجاه، لكن "اتجاه حركة الانزلاق بين الشاشات" لازم يُعكَس ليتماشى مع RTL، وليس يُنسخ كما هو من أمثلة الأداة الإنجليزية).
