# DESIGN — هوية "أثير الكوني" (Ather · Cosmic)

> عقد الهوية الملزم لتنفيذ `ATHER_COSMIC_GLASS_REDESIGN_PLAN.md` (المراحل C1→C8).
> يُقرأ هذا الملف في كل مرحلة بناء/تدقيق قادمة. المعنى والقيود والحرام هنا؛ التفاصيل الملموسة تُشغَّل في الكود وتُوثَّق باللقطات في `docs/screenshots/ather/`.

## 1. الهوية الأساسية (Identity)

- **الاسم الظاهر في كل الواجهة**: أثير
- **الاسم اللاتيني للعلامة/Splash**: Ather
- **المنتج**: مدير مكتبة ذكي للكتب الصوتية العربية + مشغّل صوتي طويل المدى. جلسات استماع تمتد لساعات، يومًا وليلًا.
- **الاستعارة المركزية**: "السماء الليلية" — قراءة هادئة وأنت تحت سماء صافية مملوءة بالنجوم. الواجهة غطاء زجاجي شبه شفاف على هذه السماء، لا جدار أمامها.
- **الطُرح الفكري (الـ why)**: الاستماع الطويل ليس عرضًا مسرحيًّا بل **جلسة إنسانية هادئة**. الواجهة أداة لتقليل الحواس وليس لمضاعفة التنبيهات: تضيء وتخفت وفق وضع الاستماع، وتختفي كلما قلّ احتياجك إليها.
- **الهدوء البصري فوق البهرجة**: راحة العين في جلسة طويلة أهم من أي لحظة "واو". كل عنصر له سبب يخدم القراءة أو التحكم، وما لا يخدم يُحذف.
- **المشهد**: واجهة زجاجية ليلية (ليل / عمق الفضاء)، تتحول فجرًا سديميًّا فاتحًا نهارًا (سديم) — **سماء بين فجر وسديم**، نجوم ثابتة وحقول سديمية بألوان النيون-المطفأة.

## 2. الأوضاع الثلاثة (The Three Modes)

| الوضع | الهوية | الخلفية (أعلى ← أسفل) |
|---|---|---|
| **سديم** (Light) | فجر دافئ حليبي، سديم بنفسجي/أرجواني خافت، نجوم قليلة باردة | `#F3EFFC` ← `#E3EEF7` (DawnTop ← DawnBottom) |
| **ليل** (Dark) — *الوضع الافتراضي* | ليلي غير أسود مطلق: حبر مزرق عميق يريح العين، سديم فاقع مطفأ، 88 نجمة | `#05060F` ← `#0B0F24` (InkTop ← InkBottom) |
| **عمق الفضاء** (AMOLED) | أسود نقي `#000000` مع أسود مائل للأزرق، أقصى كلفة ليلية، 108 نجمة أعلى تباين | `#000000` ← `#020307` |

- التبديل بين الأوضاع **فاتر دون إعادة تشغيل** وينعكس على كل الشاشات المفتوحة (مركزي عبر `ThemePreference`).
- الوضع الافتراضي للمنتج: **ليل** (طيف الاستماع الليلي هو الهوية؛ سديم مقصد صباحي وليس دخولًا أولًا).

## 3. الخطوط (Typography)

ثلاثية إلزامية، مضمّنة محليًا (OFFLINE) في `res/font/` مع تراخيص OFL في `assets/licenses/` — لا تحميل تشغيلي ولا سقوط على خط النظام:

| الخط | الدور | الأوزان المضمّنة |
|---|---|---|
| **El Messiri** | العناوين والواجهات (display/headline/title) — حضورٌ دافئ بلا بريق | 400 / 500 / 600 / 700 |
| **Cairo** | النصوص والجسم (body/label) — وضوح عربي نظيف طويل المدى | 400 / 600 / 700 |
| **Space Grotesk** | الأرقام والترميزات اللاتينية (العدادات «00:43:12»، Monospace الرقمية، ATHER) | 400 / 600 / 700 |

- `CosmicTypography` يُغطي **كل** فتحات Material 3 (displayLarge→labelSmall) بلا أي سقط إلى `FontFamily.Default`.
- الأرقام العربية/الهندية تظهر بخط Space Grotesk مع توجيه شرط الكتابة/السياق الصحيح — الأرقام قراءة، لا زخرفة.

## 4. اللوحة (Color Palette)

لوحة ثنائية "حبر/نار" مستمدة من سماء الليل؛ رمزية سالبة من العدّادات (الحبر المزرق) إيجابية من السديم (النيون-المطفأ):

**الأساس (positive):**
- Teal `#2DD4BF` — اللون الأساسي للعلامة، "نجم قطبي".
- TealBright `#45E0CC` — توهج التشغيل/الحالة النشطة.
- StardustViolet `#7C3AED` · StardustMagenta `#D946EF` · StardustAmber `#F59E0B` — ألوان السدم والحقول، مطفأة وليست صارخة.
- MoonIce `#EDF2FF` — بياض القمر للنصوص/الأيقونات فوق الحبر.

**الخلفيات:**
- InkTop `#05060F` · InkBottom `#0B0F24` — سماء ليل.
- DawnTop `#F3EFFC` · DawnBottom `#E3EEF7` — سماء سديم/فجر (وضع سديم).
- عمق الفضاء: أسود نقي + `#020307`.

## 5. الورق (Paper & Glass — Haze)

- الزجاج **حقيقي** عبر مكتبة [`dev.chrisbanes.haze`](https://github.com/chrisbanes/haze) (ضباب مصنّف بالألوان/البلورات) — لا محاكاة α-عتامة ولا بلور زائف.
- الأسطح الزجاجية: شريط تنقّل سفلي، شريط علوي متكور، بطاقات جلسة/تفاصيل، منصات أدوات المشغّل.
- شفافية/سماكة سديمية متدرجة حسب السياق: بعيدة عن اللمس → شفافة؛ قريبة (بطاقة تحكم نشطة) → أكثف.
- Fallback طبيعي على API <31 (دون `RenderEffect`): خلفية معدّلة لونيًا لطيفة، لا فراغ أسود.

## 6. القواعد الحاكمة (Non-Negotiables)

1. **لا** تدرجات بنفسجية-زرقاء `#4338CA` / `#312E81` في أي مكان.
2. **لا** عناوين وجهات ثابتة (fixed) — الهيدر يتكور/يتصاغر بالمحور (انزلاق تنقّل) ويتحول إلى شريط رقمي رفيع.
3. الزجاج حقيقي عبر haze فقط، لا عتامة مزيفة.
4. ثلاثية الخطوط El Messiri/Cairo/Space Grotesk كما في قسم 3، بلا سقوط.
5. شريط التقدم: **موجود وقابل للنقر قبل أدوات التشغيل**، في خيط زمني واحد موحّد (مزامنة موضع المكتبة)، يسبق أزرار التحكم دائمًا.
6. أدوات التشغيل الأساسية (تشغيل/إيقاف) قبل أدوات البث قبل الإضافات.
7. Reduce Motion: احترام `Settings.System.ANIMATOR_DURATION_SCALE` — الحركة تنكسر إلى ولاءات لحظية عند قيمة 0.
8. عتامة/إضاءة: Pause/Lock محفّظة دائمًا؛ أثناء تشغيل، ضبط نور تدريجي صغير.
9. وضع الاستماع مشهد انغماسي، وتصغره/تكبّره يكون جزءًا من الفعل، لا زرًا زينة.
10. كل لقطة تدقيق تتم في الأوضاع الثلاثة (سديم/ليل/عمق الفضاء).

## 7. الحرام (Hard Bans)

1. التدرج البنفسجي-الأزرق الافتراضي لكثير من التطبيقات — يُستبدل بثنائية «حبر/نار».
2. الخطوط الافتراضية العامة — تُستبدل بالثلاثية المضمّنة.
3. البطاقات المتداخلة card-in-card — طبقات مسطّحة متسلسلة.
4. النص الرمادي على خلفيات ملوّنة — تباين MoonIce-Cairo فقط.
5. الأيقونات داخل مربعات دائرية فوق كل عنوان — لا وسائد أيقونات فوق العناوين.
6. فراغات عشوائية/مضللة — مقياس 4/8dp واحترام WindowInsets حول شريط التنقل.
7. روابط عائمة بلا هوية تشبه Debug — التنقل الثانوي عنصر تصميم حقيقي.
8. مربعات ألوان فارغة كأغلفة — كل كتاب بلا صورة له Placeholder سديمي (الحرف الأول + سديم لوني مشتق من هوية أثير).
9. أي تداخل نظامي مع مدخلات المستخدم (فقاعة Autofill/Paste فوق حقل البحث) — إعداد `KeyboardOptions`/`autofillType`.
10. **الجمود**: ألا تبدو الواجهة لقطة ثابتة — الحركة الخفيفة للنجوم/النجوم الساقطة جزء من الهدوء، وليست تزينًا (مع Reduce Motion).

## 8. المبادئ المعمارية

- **Material 3 هو القاعدة، والعلامة تُعبَّر من خلاله**: حالات Material الصحيحة (Filled/Tonal/Outlined/Text)، بلا نسخ iOS ولا اختراع عناصر غريبة. الأسلوب الوظيفي (Operate) مسيطر.
- **الأغلفة والهوية اللونية**: ألوان المؤلفين/السلاسل أصلية من البيانات؛ `PlayerGradientResolver` موجود مسبقًا ويُستكمل حوله ولا يُعاد بناؤه.
- **أهداف اللمس** ≥48×48dp ومسافات ≥8dp بينها.
- **الرموز**: نص حي (sp)، وتخطيط يتحمّل تكبير الخط دون قص.
- **توقيع "أثير"**: الحرف الأول غلافًا، اللون الأساسي سديم، الحركة نجومًا هادئة — التفاصيل الدقيقة تصنع العلامة.
- **الاتجاه**: العربية RTL أولًا وأخيرًا (محاذاة، انزلاق، مواضع أزرار، قراءة). أي مبدأ LTR يُعكس ولا يُنسخ حرفيًا.

## 9. علاقة الملف بالمراحل

- **C1 (الاختبار)**: إثبات الهوية — haze + الخطوط + الخلفية + لقطة اختبارية، قبل بناء أي شاشة حقيقية.
- **C2 (الصدفة)**: الهيكل الزجاجي — هيدر متكور + تنقل زجاجي سفلي على خلفية سماوية موحّدة.
- **C3 (الأيقونة)**: أيقونة سديمية ترسم الثيمة (قمم Sin-wave من AtherSplash فوق سديم).
- **C4 (الوطن)**: إعادة بناء Home حول الجلسة الليلية على الزجاج.
- **C5 (المشغّل)**: خيط زمني موحّد + إضاءة ليلية — بوابة STOP على لقطات السيناريو.
- **C6 (البقية)**: BookDetails/Library/Settings وكل الشاشات بمادة الزجاج.
- **C7 (الحركة)**: نجوم حيّة + حركة هادئة + Reduce Motion (STOP الثاني).
- **C8 (التدقيق النهائي)**: E2E في الأوضاع الثلاثة مقابل لقطات ما قبل الكوني، وتوثيق الجداول.