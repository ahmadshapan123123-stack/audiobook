# أثير الكوني (Ather Cosmic) — إعادة تصميم جذرية كاملة + خطة تنفيذ صارمة

هذا الملف **يحل محل توجيه Prompt 5 وPrompt 6** في `ATHER_REDESIGN_IMPECCABLE_PROMPTS.md` (اللي طلعت نتيجته لسه Material عام حسب اللقطات الجديدة). Prompt 1-4 و2.5/2.6 خلاص خلّصوا وصالحين — منبنيش فوقهم من الصفر، بس التوجيه البصري بقى مختلف تمامًا من هنا.

**لا يُلغي هذا الملف أي MUST في `AUDIOBOOK_APP_SPEC.md v2`**: RTL كامل، الأوضاع الثلاثة (وهتتحول لمفهوم "ليل كوني/نهار كوني/أسود عميق" بدل Light/Dark/AMOLED التقليدي، بنفس الوظيفة)، عناصر تحكم الـPlayer المذكورة حرفيًا، التطبيع العربي للبحث.

---

## تشخيص دقيق من اللقطات الست الأخيرة (مرجع إلزامي)

**إيجابيات حقيقية تُحفظ ولا تُهدم:**
- أغلفة بحرف أول + تدرج لوني (`ب`، `ا`، `م`، `أ`) في Library وHome وBookDetails — فكرة صحيحة، تُطوَّر بصريًا مش تُلغى.
- حالة "لم نجد نتائج" في `p6_library_noresults.png` — منسّقة ومكتوبة كويس، تُحفظ كما هي وظيفيًا مع تحديث بصري فقط.
- تبويبات BookDetails (البيانات/الإصدارات/المحتوى) وحقول العنوان/المؤلف القابلة للتعديل في `p7_bookdetails.png` — بنية وظيفية صحيحة.

**أعطال حرجة جديدة في Player (`p7_player.png`) — أولوية قصوى:**
- **تايم لاين مكرر فعليًا**: شريط "35:00 — 00:00" مع نقطة تقدّم رفيعة، **ثم** صندوق منفصل تحته "نظرة عامة" فيه أرقام "4 3 2 1" عايمة بلا أي خط زمني واضح تربطها، ونقطة واحدة تانية على اليمين. دول عنصرين مختلفين بيمثلوا نفس المفهوم (موضع التشغيل/الفصول) بطريقتين متضاربتين بصريًا. **الحل: عنصر واحد فقط** — Timeline حقيقي تفاعلي (Scrubbing شغال فعليًا، مش شريط ميت) بعلامات فصول مدمجة عليه مباشرة، مش صندوق منفصل.
- غلاف الكتاب مجرد **مستطيل بنفسجي غامق فاضي بعنوان نصي فقط** — مفيش أي هوية بصرية حقيقية (لا حرف أول، لا نمط، لا توهج).
- زرار "تكبير ٣٠-٦٠ دقيقة" شكله Outline عادي جنب زرار "نظرة عامة" شكله Filled مختلف تمامًا — نفس المفهوم (تبديل عرض) بستايلين متضاربين.
- الخلفية لسه تدرج بنفسجي-أزرق كلاسيكي (نفس ATH-16 القديم لم يُحل بصريًا، حتى لو الكود بقى "ديناميكي").

**أعطال Home (`p6_home_light.png`):**
- بعد الكارت والإحصائيات، قسم "أقسام المكتبة" **مجرد أزرار تنقل فاضية** (أستمع الآن، مكتملة، المفضلة، المجموعات) — بدون عرض أي محتوى فعلي (لا أغلفة، لا معاينة). هذا يخالف روح "الصفحة الرئيسية" في أي تطبيق حقيقي مشابه (Spotify, Audible, Storytel): الصفحة الرئيسية بتعرض محتوى فعلي (أغلفة، صفوف أفقية) مش بس بوابات نص.

**أعطال بنيوية عامة (كل الشاشات):** رأس ثابت كبير ياكل مساحة، خلفية مسطحة بلا عمق، لا تأثيرات زجاجية، خط Typography لسه شكله افتراضي عام.

---

## هوية التصميم الجديدة: "أثير الكوني"

المفهوم: التطبيق يُشعر المستخدم إنه بيستمع لقصة تحت سماء ليلية مليانة نجوم وسدم — العتمة الكونية الهادئة تناسب الاستماع الليلي الطويل (المطلوب أصلاً في قسم 13 من الـSpec)، والسطوح الزجاجية العائمة فوقها بتدّي إحساس "شاشات مضيئة" بتطفو في الفضاء، وده بيلمّح لعالم القصة اللي بيسمعها المستخدم.

### 1. نظام الألوان
- **الخلفية الأساسية (كل الأوضاع)**: تدرج كوني عميق شبه ثابت من نيلي شبه أسود (`#05060F`) إلى كحلي غامق (`#0B0F24`)، مع طبقة نجوم/جسيمات دقيقة مرسومة بـCanvas (كثافة منخفضة، حركة بطيئة جدًا شبه ثابتة — انتباه لـReduce Motion).
- **لون Ather الأساسي**: تيل/فيروزي (زي الـSplash الحالي بالظبط `p3_splash_ather.png` — ده صحيح ويُحفظ) كلون الهوية الرئيسي (الأزرار، العناصر التفاعلية).
- **ألوان "غبار النجوم" (Stardust) كتدرجات ثانوية نادرة**: بنفسجي كوني (`#7C3AED`) → ماجنتا سديمي (`#D946EF`) → كهرماني (`#F59E0B`) — تُستخدم في التدرجات الديناميكية (أغلفة الكتب، خلفية الـPlayer) بدل التدرج البنفسجي-الأزرق الكلاسيكي الحالي، بحيث كل كتاب/مؤلف عنده "لون سديمي" مميز مبني على نفس منطق `PlayerGradientResolver` الموجود لكن بلوحة ألوان جديدة كليًا (ممنوع `#4338CA`/`#312E81` القديمين).
- **الأوضاع الثلاثة تتحول لمفهوم**: "سديم" (فاتح، خلفية سماء فجر بلون كوني فاتح جدًا بدل أبيض ناصع) / "ليل" (الوضع الافتراضي، الخلفية الكونية الموصوفة فوق) / "عمق الفضاء" (أسود نقي #000000 حقيقي لـAMOLED، مع نجوم أوضح تباينًا). لازم تحقق نفس متطلبات التباين القديمة، بس بهوية كونية بدل رمادي-مزرق عادي.

### 2. الطباعة (Typography)
- **العناوين الكبيرة (Display/Headline بالعربي)**: خط **El Messiri** (من Google Fonts) — شخصية أدبية دافئة مميزة، بديل قوي عن أي خط افتراضي.
- **النصوص العادية/الواجهة (Body/Label بالعربي)**: خط **Cairo** (Variable Font) — هندسي نظيف، مقروء بحجم صغير، ومتناغم مع اللاتيني.
- **الأرقام والعلامة اللاتينية "ATHER"**: خط **Space Grotesk** — هندسي عصري، وتماشيه مع اسمه "Space" مناسب لهوية كونية.
- استخدم Google Fonts Downloadable Fonts API في Compose (`androidx.compose.ui.text.googlefonts`) أو ضمّن الخطوط كـ `res/font/` مباشرة لضمان توفرها بدون اتصال (Offline-first حسب MUST في الـSpec — **[MUST] لا تعتمد على تحميل الخط أونلاين وقت التشغيل، ضمّنه كملف داخل التطبيق**).

### 3. الزجاجية (Glassmorphism) — تقنية حقيقية لا تقليد لوني
- أضف مكتبة **Haze** (`dev.chrisbanes.haze`) — هي المعيار الفعلي لعمل Blur حقيقي خلف العناصر في Compose.
- **[MUST] ممنوع محاكاة الزجاجية بلون شبه شفاف بسيط (`Color.White.copy(alpha=0.1f)`) بدون Blur حقيقي** — ده تقليد سطحي مش زجاجية فعلية، ولازم يُرفض في أي مراجعة.
- استخدم Haze تحديدًا على: شريط التنقل السفلي، المشغّل المصغّر (Mini Player لو موجود)، أي Bottom Sheet، أي Overlay فوق محتوى قابل للتمرير.

### 4. الحركة (Motion)
- استخدم نظام Material 3 Expressive الجديد في Compose (Spring-based motion، مش Duration/Easing تقليدي) للانتقالات بين الشاشات والعناصر التفاعلية (ضغط زر، فتح بطاقة).
- أضف حركة خلفية خفيفة جدًا للسدم/النجوم (Canvas مع `infiniteRepeatable` بطيء جدًا) — **[MUST]** توقف تلقائيًا أو تتحول لحالة ثابتة عند تفعيل "تقليل الحركة" في النظام (ATH-P05 من التدقيق السابق، لا يزال ساريًا هنا وأشد أهمية الآن).

### 5. لا رأس علوي ثابت (No Fixed Top Bar)
- احذف الرأس الثابت الكبير (العنوان + الوصف الثابتين فوق كل شاشة) في كل مكان.
- بدّله بـ**رأس منسدل/متلاشٍ مع التمرير (Collapsing Header)**: يبدأ كبيرًا وبارزًا أعلى المحتوى القابل للتمرير، ويتقلّص/يتلاشى تدريجيًا لعنوان صغير مدمج مع Blur خفيف خلفه لما المستخدم يعمل Scroll لأسفل — استخدم `LargeTopAppBar` من Material3 كنقطة بداية، مُعدَّل بهوية أثير الكونية (شفاف فوق الخلفية الكونية، يتحول لسطح زجاجي عند الالتصاق بالأعلى فقط).

### 6. شريط تنقل سفلي زجاجي (Glass Bottom Nav)
- `NavigationBar` عائم (مش ملاصق للحواف بالكامل) بـHaze Blur حقيقي خلفه، حواف دائرية، هامش صغير عن حواف الشاشة، يحترم `navigationBars` inset (المُصلَح فعلاً من Prompt 2.6).

---

## أيقونة التطبيق (App Icon)

Adaptive Icon بطبقتين:
- **الخلفية**: تدرج سديمي (بنفسجي → تيل → كحلي) يطابق خلفية الـSplash.
- **المقدمة**: رمز مبسّط يدمج بين شكل موجة صوت (Soundwave، زي الشعار الحالي في الـSplash بالظبط) وهلال/نجمة صغيرة أو مسار مداري خفيف حولها — يعبّر عن "صوت + فضاء" في رمز واحد بسيط يشتغل بحجم صغير (48dp) بوضوح.
- صمّمها كـ Vector Drawable (لا صورة نقطية) لضمان الحدة على كل الدقة.

---

## Home: إعادة بناء لعرض محتوى حقيقي (لا بوابات فاضية)

بدل الأزرار الفاضية الحالية، الصفحة الرئيسية لازم تعرض **محتوى فعلي** (بنفس منطق تطبيقات صوتية مشابهة: الصفحة الرئيسية واجهة عرض، مش قائمة تنقل):

1. ترحيب ديناميكي حسب الوقت ("مساء أثيري" ليلاً، "صباح هادئ" صباحًا — استخدم لمسة من هوية أثير بدل "أهلًا بك" العام).
2. بطاقة "متابعة الاستماع" الحالية — تُطوَّر بصريًا بغلاف حقيقي بارز بدل مستطيل مسطح.
3. **صف أفقي حقيقي "قيد الاستماع"**: كل الكتب IN_PROGRESS (مش كتاب واحد بس)، بأغلفتها الفعلية، قابل للتمرير أفقيًا.
4. **صف أفقي حقيقي "أُضيف حديثًا"**: آخر الكتب المضافة بأغلفتها.
5. **صف أفقي "من مفضلتك"**: عيّنة من كتب Favorites بأغلفتها.
6. إحصائيات موجزة (تُحفظ لكن تُعاد تصميمها كشرائح زجاجية صغيرة، مش مربعات بيضاء مسطحة).
7. اختصارات الأقسام (مكتملة/المجموعات) تُحفظ **كصف أخير فقط**، مش كل الصفحة.

**[MUST] كل صف فوق يعرض بيانات Room حقيقية من بذور البيانات — ولو قسم فاضي (مفيش Favorites مثلاً) يظهر بحالة فارغة مصمَّمة، مش يختفي بصمت أو يفضل بوابة نص فاضية.**

---

## Player: إعادة بناء كاملة (أولوية قصوى، أكثر شاشة تحتاج دقة)

**المرجع الدقيق للأعطال المطلوب حلها (من التدقيق السابق + اللقطة الجديدة):**

1. **وحّد التايم لاين في عنصر واحد فقط.** احذف صندوق "نظرة عامة" المنفصل بالأرقام العائمة تمامًا. ابنِ Timeline واحد تفاعلي حقيقي:
   - يستخدم `AppProgressSlider` الموجود بالفعل في `AppComponents.kt:59-68` وغير المستخدم حاليًا (ATH-05) — فعّله كعنصر Scrubbing حقيقي (اسحب لتغيير الموضع، ليس شريطًا ميتًا).
   - علامات الفصول (Chapter Markers) تُرسم **كنقاط/خطوط صغيرة فوق نفس الشريط مباشرة**، برقم الفصل يظهر كـ Tooltip عند اللمس/التمرير، مش في صندوق منفصل تحته.
   - مستويان (Overview كامل الكتاب / Zoomed 30-60 دقيقة) يبقيان، لكن كتبديل سلس (Pinch-to-zoom أو زر واحد بأيقونة، مش زرارين بستايلين مختلفين) على **نفس** الـTimeline، مش شاشتين مختلفتين الشكل.
2. **أعد تصميم منطقة الغلاف بالكامل**: بدل المستطيل الفارغ، استخدم نفس نظام "الحرف الأول + تدرج سديمي" المطوَّر من Library/BookDetails، بحجم كبير بارز، مع توهج خفيف (Glow) حول حوافه بلون التدرج نفسه يمتزج مع خلفية الـPlayer الكونية.
3. **وحّد ستايل زرار Zoom/Overview**: عنصر تبديل واحد (Segmented Control زجاجي) بستايل واحد متسق، لا زرارين مختلفين.
4. **أعد تصميم شريط التحكم السفلي كشريط زجاجي عائم** (Haze) بدل خلفية شفافة عادية، يحتوي بالضبط العناصر المذكورة حرفيًا في قسم 10 من الـSpec (Previous Chapter | -15s | Play/Pause | +15s | Next Chapter + Speed/Sleep Timer/More) — لا أكثر ولا أقل.
5. الخلفية: طبّق لوحة "غبار النجوم" الجديدة بدل التدرج البنفسجي-الأزرق الحالي.

**[بوابة قبول]**: بعد إعادة البناء، لو فيه أي عنصرين مختلفين بيعرضوا نفس معلومة الموضع/الفصل الحالي في نفس الوقت على الشاشة، الشاشة مرفوضة تلقائيًا — عنصر واحد فقط لكل معلومة.

---

## باقي الشاشات: تطبيق نفس الهوية

- Library: احذف الرأس الثابت الكبير (نفس مبدأ Home)، طبّق الخلفية الكونية والزجاجية على شريط البحث والـChips.
- BookDetails: أبقِ البنية الوظيفية (تبويبات، حقول تعديل) لكنها تُلبَس هوية أثير الكونية بالكامل (الخلفية، الأغلفة المتوهجة، الأزرار الزجاجية).
- Settings/Statistics/History/Bookmarks/ScanReview/LibraryRoots: نفس نظام الألوان والزجاجية والطباعة، بلا استثناء — **[MUST] لا شاشة منسية بستايل Material افتراضي قديم**.

---

## قائمة القواعد غير القابلة للتفاوض (Non-Negotiable)

- [MUST] ممنوع أي استخدام لخط Roboto الافتراضي أو أي خط نظام عام — El Messiri/Cairo/Space Grotesk فقط، مضمَّنين محليًا.
- [MUST] الزجاجية = Haze Blur حقيقي، مش لون شبه شفاف.
- [MUST] لا رأس ثابت كبير في أي شاشة تحتوي محتوى قابل للتمرير.
- [MUST] شريط تنقل سفلي زجاجي عائم في كل الشاشات الرئيسية.
- [MUST] Player: عنصر Timeline واحد فقط، لا تكرار إطلاقًا.
- [MUST] Home: كل صف يعرض بيانات حقيقية، صفر أزرار تنقل فاضية بلا معاينة محتوى.
- [MUST] أيقونة تطبيق مخصصة حقيقية (Vector، Adaptive) — لا Placeholder افتراضي من Android Studio.
- [MUST] الحركة الخلفية (نجوم/سدم) تحترم إعداد "تقليل الحركة" في النظام.
- [MUST] كل التزامات `AUDIOBOOK_APP_SPEC.md v2` (RTL، الأوضاع الثلاثة وظيفيًا، عناصر التحكم المحددة حرفيًا) تظل سارية تحت الهوية الجديدة.

---

## تسلسل التنفيذ الصارم (استخدم Impeccable حيث ينطبق + كود مباشر حيث لا ينطبق)

### Prompt C1 — تثبيت الأساسيات: Haze + الخطوط + DESIGN.md الكوني

```
اقرأ AUDIOBOOK_APP_SPEC.md v2 وكل ملفات الخطة السابقة (خصوصًا ATHER_REDESIGN_IMPECCABLE_PROMPTS.md) وهذا الملف كاملاً أولاً — هذا الملف يحل محل التوجيه البصري لـPrompt 5/6 القديم بالكامل، بينما Prompt 1-4 وPrompt 2.5/2.6 تبقى منجزة ومعتمدة.

1. أضف مكتبة Haze (`dev.chrisbanes.haze`, أحدث إصدار مستقر) لملف build.gradle.kts.
2. حمّل خطوط El Messiri وCairo (Variable) وSpace Grotesk وضمّنها كملفات في res/font/ (لا تعتمد على تحميل أونلاين وقت التشغيل — Offline-first إلزامي). اضبط Typography الافتراضي في Compose Theme ليستخدم El Messiri للعناوين، Cairo للنصوص العادية، Space Grotesk للأرقام/الشعار اللاتيني.
3. أعد كتابة DESIGN.md بالكامل بمحتوى قسم "هوية التصميم الجديدة: أثير الكوني" أعلاه — الألوان، الطباعة، الزجاجية، الحركة، قاعدة "لا رأس ثابت" — هذا يحل محل أي DESIGN.md سابق كتبه Impeccable في Prompt 1 القديم.
4. اختبر على شاشة واحدة بسيطة: خلفية كونية متدرجة + طبقة نجوم ساكنة (بدون حركة بعد) + نص بخط El Messiri وCairo ظاهر بوضوح. خذ لقطة فعلية.

توقف واعرض DESIGN.md الجديد كاملاً + لقطة الاختبار، قبل أي بناء فعلي لأي شاشة.
```

### Prompt C2 — الأساس البنيوي العام: لا رأس ثابت + شريط تنقل زجاجي

```
هذا تعديل على مستوى الـShell العام (MainActivity/NavHost) يطبَّق مرة واحدة، قبل إعادة بناء أي شاشة فردية — بنفس منطق إصلاح WindowInsets في Prompt 2.5.

1. احذف نمط "الرأس الثابت الكبير" (عنوان + وصف ثابتين أعلى الشاشة بلا تمرير) من المكوّن المشترك المستخدم عبر الشاشات، وبدّله بمكوّن Collapsing Header قابل لإعادة الاستخدام (Large title يتقلص مع Scroll إلى شريط مضغوط بخلفية زجاجية Haze عند الالتصاق بالأعلى).
2. أعد بناء شريط التنقل السفلي كعنصر عائم بحواف دائرية وهامش عن الحواف، بخلفية Haze Blur حقيقية، محترمًا WindowInsets (تأكد عدم كسر إصلاح Prompt 2.5/2.6).
3. طبّق الخلفية الكونية المتدرجة (بلا حركة بعد) كخلفية أساسية موحدة خلف كل الشاشات عبر الـShell، بدل خلفية كل شاشة المنفصلة.
4. شغّل على المحاكي، تنقّل بين كل الشاشات، وتأكد أن كل شاشة تستخدم المكوّن الجديد للرأس والشريط السفلي بلا استثناء.

توقف واعرض لقطات فعلية لثلاث شاشات مختلفة على الأقل تُظهر الرأس المنسدل والشريط الزجاجي شغالين.
```

### Prompt C3 — أيقونة التطبيق

```
صمّم أيقونة Adaptive Icon حقيقية حسب "أيقونة التطبيق" أعلاه: طبقة خلفية بتدرج سديمي، طبقة مقدمة برمز موجة صوت + هلال/مدار بسيط، كـVector Drawable. اضبطها كأيقونة التطبيق الرسمية (ic_launcher) بكل الأحجام المطلوبة. اختبر ظهورها فعليًا في Launcher/App Drawer على المحاكي وخذ لقطة.
```

### Prompt C4 — إعادة بناء Home بمحتوى حقيقي

```
نفّذ "Home: إعادة بناء لعرض محتوى حقيقي" أعلاه حرفيًا: ترحيب ديناميكي، بطاقة متابعة استماع مطوَّرة، 3 صفوف أفقية حقيقية (قيد الاستماع/أُضيف حديثًا/المفضلة) ببيانات Room فعلية من بذور البيانات، إحصائيات كشرائح زجاجية، اختصارات الأقسام كصف أخير فقط. اختبر بصريًا على المحاكي، وتأكد أن كل صف يعرض أغلفة حقيقية متعددة وليس بوابة نص فاضية. خذ لقطة كاملة (Scroll لأسفل حتى آخر الصفحة) في وضعي "ليل" و"سديم".

توقف واعرض اللقطات + تأكيد أن كل قسم مربوط ببيانات حقيقية لا نص Placeholder.
```

### Prompt C5 — إعادة بناء Player الكاملة (أدق وأهم خطوة)

```
نفّذ قسم "Player: إعادة بناء كاملة" أعلاه بالتفصيل الحرفي، نقطة نقطة:

1. احذف صندوق "نظرة عامة" المنفصل بالأرقام العائمة نهائيًا من الكود.
2. فعّل AppProgressSlider (AppComponents.kt:59-68) كعنصر Scrubbing حقيقي متصل فعليًا بـPlaybackController (اسحب أثناء التشغيل ويتغير الموضع فعليًا في الصوت).
3. ارسم علامات الفصول كنقاط/خطوط فوق نفس الـSlider، مع Tooltip لرقم الفصل عند التمرير/اللمس.
4. وحّد وضعي Overview/Zoomed في عنصر تبديل زجاجي واحد بستايل متسق (لا زرارين مختلفي الشكل).
5. أعد تصميم منطقة الغلاف: حرف أول + تدرج سديمي + توهج، بدل المستطيل الفارغ.
6. أعد تصميم شريط التحكم السفلي كشريط زجاجي عائم بعناصر قسم 10 من الـSpec بالحرف، لا أكثر ولا أقل.
7. طبّق خلفية "غبار النجوم" بدل التدرج البنفسجي-الأزرق القديم.

**بوابة قبول إلزامية**: صوّر الشاشة النهائية وتأكد بعينك أن معلومة "الموضع الحالي/الفصل الحالي" تظهر مرة واحدة فقط، مش في مكانين مختلفين. لو ظهرت مرتين، الخطوة مرفوضة وأعد العمل.

اختبر فعليًا بالصوت الحقيقي: شغّل كتابًا متعدد الفصول، اسحب على الـTimeline لمواضع مختلفة، وتأكد أن الصوت يقفز فعليًا لنفس الموضع. خذ لقطة نهائية.
```

### Prompt C6 — تعميم الهوية على باقي الشاشات

```
طبّق نفس نظام الألوان/الطباعة/الزجاجية/الرأس المنسدل على: Library, BookDetails, Settings, Statistics, History, Bookmarks, ScanReview, LibraryRoots — شاشة شاشة، بنفس منهجية craft→polish→audit من الملف السابق، مع الحفاظ الصريح على البنية الوظيفية الموجودة بالفعل في BookDetails (التبويبات، حقول التعديل) وحالة "لا نتائج" في Library. لا شاشة تُترك بستايل Material قديم.

توقف بعد كل شاشة واعرض لقطة فعلية.
```

### Prompt C7 — طبقة الحركة (Motion Pass)

```
1. فعّل نظام Material 3 Expressive Motion (Spring-based) للانتقالات بين الشاشات والتفاعلات (ضغط زر، فتح بطاقة، توسيع Timeline).
2. أضف حركة خلفية خفيفة جدًا للنجوم/السدم (لمعان بطيء، حركة بالكاد ملحوظة) عبر Canvas + `infiniteRepeatable`، بأداء خفيف (قِس معدل الإطارات، لا تُسقط الأداء).
3. اربطها بإعداد "تقليل الحركة" في النظام: عند تفعيله، الخلفية تتجمد على إطار ثابت والانتقالات تتحول لتلاشي بسيط بدل Springs.
4. اختبر الأداء (لا كتاكيك/تقطيع ملحوظ) على المحاكي خصوصًا في شاشة Player أثناء التشغيل الفعلي.

توقف واعرض فيديو قصير أو وصف دقيق للحركة المُختبرة فعليًا + نتيجة اختبار Reduce Motion.
```

### Prompt C8 — بوابة الإغلاق النهائية

```
1. أعد تشغيل جولة End-to-End كاملة على المحاكي بالهوية الجديدة، سجّل فيديو.
2. قارن صراحة كل شاشة بلقطتها القديمة المرفقة هنا بالاسم (p6_home_light.png, p6_library_grid.png, p6_library_noresults.png, p7_player.png, p7_bookdetails.png, p3_splash_ather.png) — لو أي شاشة لسه شبه القديمة بشكل ملحوظ، مرفوضة.
3. راجع "قائمة القواعد غير القابلة للتفاوض" أعلاه بندًا بندًا، بدليل لقطة/كود لكل بند.
4. راجع كل عناصر ATH-01 إلى ATH-30 وATH-P01 إلى ATH-P05 من الجولة السابقة — تأكد أن إعادة البناء دي ما كسرتش أي إصلاح سابق (خصوصًا ATH-01 Insets وATH-05 Scrubbing اللي أصبحوا جزءًا أساسيًا من هذه المرحلة).
5. اكتب تقرير ختامي بجدول: كل بند من القواعد + كل ATH + حالة ✓/✗ + دليل.

لا تُغلق هذه المرحلة بدون هذا التقرير الكامل مع الأدلة المرفقة فعليًا.
```
