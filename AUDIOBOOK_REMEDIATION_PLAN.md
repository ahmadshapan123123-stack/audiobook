# خطة إصلاح تطبيق Audiobook Player — النسخة R1
## (Remediation Plan — مبنية على تدقيق كودي فعلي، وليس افتراضًا)

هذا الملف **مكمّل** لـ `AUDIOBOOK_APP_SPEC.md v2` ولا يلغيه: الـSpec الأصلي يظل المرجع الوحيد لقواعد المنتج (MUST/SHOULD/FUTURE). هذا الملف يضيف طبقة ثانية إلزامية: **حالة الكود الفعلية** كما تم التحقق منها بالفحص المباشر، وخطة مراحل جديدة (R0–R6) لإغلاق الفجوة بينهما.

عند أي تعارض: الأولوية دائمًا لـ`AUDIOBOOK_APP_SPEC.md v2`. هذا الملف لا يضيف أي فيتشر جديد، فقط يُلزم بإكمال ما هو موجود في الـSpec بشكل صحيح ومُثبت.

---

## ملخص نتائج التدقيق الفعلي (Ground Truth)

تم فحص المستودع كاملاً (37 ملف Kotlin رئيسي + كل ملفات الاختبار) بالقراءة المباشرة والبحث النصي المستهدف على كل قرار MUST. النتائج:

| العنصر | الحالة الموثّقة بالكود |
|---|---|
| `ScanRoot.ensureEdition()` | يُنشئ إصدارًا واحدًا لكل مجلد بـ`confidenceScore = 1f` ثابت حرفيًا. لا استخراج إشارات، لا مستويات ذكاء، لا استخدام لـ`EditionMatchDecision` (الجدول موجود وفارغ دائمًا). |
| `ExoPlaybackController.setSleepTimer()` | عدّاد تنازلي + `pause()`. لا نافذة تحذير، لا Ducking، لا تمديد، لا Active Interaction، لا Fade Out، لا تسجيل `ListeningSession`. |
| `StatisticsRepository` | دالة واحدة `completedSessions()` بلا أي حساب لـStreak أو نسبة اكتمال الفصل أو متوسط السرعة. لا شاشة Statistics أو History. |
| `contentDescription` في كامل المشروع | 0 نتيجة. |
| `MainActivity` | لا يوجد `NavHost`، لا ViewModel حقيقي لـLibrary/BookDetails، التنقل كله يدور حول `demoEditionId` ثابت واحد. |
| `LibraryScreen` / `BookDetailsScreen` | بيانات `sampleBooks` / `sampleEdition` مكتوبة يدويًا (Hardcoded)، غير متصلة بأي Repository. |
| مجلد `androidTest` | غير موجود إطلاقًا. كل الاختبارات JVM unit tests صغيرة (11–48 سطرًا). |
| الغلاف الديناميكي (Gradient) ومعالجة الملف المفقود أثناء التشغيل | **منفَّذان بشكل صحيح ومطابق للـSpec** — نقطتان إيجابيتان مؤكَّدتان. |

**الخلاصة:** الهيكل المعماري (Phase 0/1/2/4) صلب وحقيقي. لكن "الذكاء" الذي هو جوهر فكرة المنتج (قسم 1: *"مدير مكتبة ذكي، ليس مجرد مشغل صوت"*) غير موجود فعليًا في ثلاث نقاط جوهرية: اكتشاف الإصدارات، المؤقت الذكي، والربط ببيانات حقيقية.

---

## قاعدة إلزامية جديدة تسري على كل مرحلة قادمة: بروتوكول الإثبات الصادق

المراحل السابقة (Phase 0–12 الأصلية) تضمنت طلبات مثل *"اختبر فعليًا على جهاز حقيقي"* و*"شغّل TalkBack وتأكد"*. تم التحقق أن **لا بنية اختبار على جهاز (`androidTest`) موجودة إطلاقًا** في المستودع — أي أن أي تقرير سابق ادّعى ذلك لم يكن دليله كافيًا. لمنع تكرار هذا:

**[MUST] لكل نقطة "اختبار" في أي برومبت أدناه، الدليل المقبول الوحيد هو واحد مما يلي، وبصراحة تامة:**
1. **اختبار وحدة (unit test) حقيقي** — يُرفق: كود الاختبار + أمر التشغيل (`./gradlew test`) + المخرجات الحرفية (عدد الاختبارات الناجحة/الفاشلة).
2. **اختبار Instrumented/Compose UI حقيقي** (داخل `androidTest`) — إن كانت البيئة تسمح بتشغيله فعليًا على محاكي/جهاز، يُرفق دليل التشغيل الفعلي. **إن لم تكن البيئة تسمح بذلك (لا محاكي متاح)، يُصرَّح بهذا صراحة في التقرير** بدل الادعاء بأنه "اختُبر على جهاز".
3. **مراجعة كود موثّقة (code walkthrough)** — فقط للنقاط التي لا يمكن اختبارها آليًا (مثل تناسق بصري)، وتُذكر صراحة كـ"مراجعة كود يدوية" وليس "اختبار".

**[MUST] ممنوع** أن يحتوي أي تقنون تنفيذ عبارات من نوع "تأكدت فعليًا على الجهاز" أو "شغّلت TalkBack" ما لم يُرفق دليل تشغيل فعلي قابل للتحقق. الصياغة الصادقة ("لم تتوفر بيئة تشغيل فعلية، تم التحقق عبر اختبار Instrumented/Robolectric بدلاً من ذلك") مقبولة دائمًا وأفضل من ادعاء غير مدعوم.

**[MUST] لا انحدار (No Regression):** في نهاية كل مرحلة من R0–R6، تُعاد تشغيل **كل** اختبارات المراحل السابقة (بما فيها Phase 0–12 الأصلية) ويُثبت نجاحها قبل اعتبار المرحلة الجديدة منتهية.

---

## Phase R0 — تجميد الأرضية + بنية اختبار حقيقية أولاً

قبل أي إصلاح منطقي، يجب إيقاف تراكم الديون التقنية والبدء ببنية اختبار حقيقية.

```
اقرأ AUDIOBOOK_APP_SPEC.md v2 وAUDIOBOOK_REMEDIATION_PLAN.md كاملين قبل أي سطر كود. أنت مهندس أندرويد سنيور مكلّف بإصلاح مشروع موجود، وليس ببنائه من الصفر — لا تعد كتابة أي جزء يعمل بشكل صحيح ومُثبت في جدول "ملخص نتائج التدقيق الفعلي".

المطلوب في هذه المرحلة فقط:

1. أضف موديول/مصدر `androidTest` فعليًا للمشروع (Compose UI testing + AndroidJUnit4)، بما يشمل الإعداد الكامل في build.gradle.kts (dependencies: androidx.compose.ui:ui-test-junit4, androidx.test.ext:junit, إلخ).
2. اكتب اختبار Instrumented واحد بسيط (Smoke Test) يثبت أن التطبيق يُقلع ويعرض LibraryScreen دون Crash — هذا أول اختبار حقيقي على مستوى الجهاز/المحاكي في تاريخ المشروع.
3. طبّق "بروتوكول الإثبات الصادق" حرفيًا من الآن: أي تقرير تكتبه من هذه المرحلة فصاعدًا يجب أن يحدد بوضوح أي من الأنواع الثلاثة (unit / instrumented فعلي / مراجعة كود) استخدمته لكل ادعاء، ولا يُسمح بصياغات غامضة مثل "تم التأكد".
4. شغّل **كل** اختبارات JVM الموجودة حاليًا في `app/src/test` (13 ملفًا) وأرفق النتيجة الحرفية لـ`./gradlew test` كخط أساس (Baseline) قبل أي تعديل قادم — هذا مرجع لاكتشاف أي انحدار لاحقًا.
5. لا تُصلح أي منطق فعلي في هذه المرحلة (لا Edition Detection، لا Sleep Timer). فقط البنية التحتية للاختبار والخط الأساسي.

توقف بعد كل نقطة واعرض: الكود، أمر التشغيل الفعلي، والمخرجات الحرفية. إن تعذّر تشغيل Instrumented test فعليًا في بيئتك، صرّح بذلك بوضوح تام في التقرير بدل الادعاء بخلافه.
```

---

## Phase R1 — إعادة هيكلة التنقل وربط البيانات الحقيقية (Wiring)

هذه المرحلة **شرط مسبق** لكل ما بعدها — لا فائدة من إصلاح Edition Detection أو Statistics إن كانت الشاشات لا تزال تعرض بيانات وهمية بمعرّف ثابت واحد.

```
بناءً على قسم "مميزات المكتبة" و"Book Details — مركز إدارة الكتاب" في AUDIOBOOK_APP_SPEC.md v2، وبعد تأكيد نجاح Phase R0:

1. **أعد بناء التنقل في MainActivity بالكامل عبر Navigation Compose (NavHost) حقيقي**، بمسارات تحمل معرّفات حقيقية:
   - `library` → `book_details/{bookId}` → `player/{editionId}` → `bookmarks/{editionId}`
   - احذف الاعتماد على `demoEditionId` كمعرّف تنقل وحيد. يجب أن يفتح كل كتاب في المكتبة صفحة تفاصيله الخاصة به فعليًا، وليس نفس الكتاب الوهمي دائمًا.

2. **أنشئ ViewModels حقيقية** (Hilt `@HiltViewModel`) تستهلك الـRepositories الموجودة فعليًا (BookRepository, EditionRepository, ProgressRepository, إلخ) بدلاً من القوائم اليدوية:
   - `LibraryViewModel`: يجلب الكتب من `BookRepository` مع التقدم من `ProgressRepository`، ويطبّق منطق البحث/الفرز/الفلترة الموجود فعلاً في `LibraryQuery.kt` (لا تُعد كتابته، فقط وصّله ببيانات حقيقية بدل `sampleBooks`).
   - `BookDetailsViewModel`: يستقبل `bookId` من التنقل، ويجلب Book/Author/Series/Editions/Chapters/Bookmarks/Progress الحقيقيين. احذف `sampleEdition()` من مسار الإنتاج (يمكن الإبقاء عليها فقط داخل دالة `@Preview` منفصلة معزولة عن أي كود تشغيل حقيقي).
   - `BookmarksViewModel` إن لزم، بنفس المبدأ.

3. **حوّل `EnsureDemoEdition` إلى أداة Seed تطويرية صريحة** خلف علم `BuildConfig.DEBUG` أو مشابه، موثّقة في تعليق بأنها ليست جزءًا من مسار المستخدم الحقيقي، وتُستخدم فقط لتعبئة قاعدة بيانات فارغة أثناء التطوير المحلي، لا لعرضها كأنها بيانات المستخدم.

4. **احذف `sampleBooks` من `LibraryScreen.kt`** بعد الربط الكامل، أو انقلها لملف Preview منفصل غير مستخدم في مسار التشغيل.

5. اكتب اختبار Instrumented حقيقي (باستخدام بنية Phase R0): افتح المكتبة ببيانات مُدخلة فعليًا عبر Room in-memory (كتابان مختلفان بعنوانين مختلفين)، اضغط على الكتاب الثاني، وتأكد أن BookDetailsScreen يعرض بيانات الكتاب الثاني تحديدًا وليس الأول أو بيانات وهمية.

توقف بعد كل نقطة، شغّل الاختبار الجديد + كل اختبارات Phase R0/الأصلية القديمة (No Regression)، واعرض النتائج الحرفية.
```

---

## Phase R2 — Smart Edition Detection: التنفيذ الحقيقي الكامل

```
بناءً على قسم "Smart Edition Detection — النسخة المُحصّنة" في AUDIOBOOK_APP_SPEC.md v2. الكود الحالي في ScanRoot.ensureEdition() ينشئ إصدارًا واحدًا بثقة ثابتة 1f بلا أي استخراج إشارات فعلي — هذا استبدال جوهري وليس تحسينًا طفيفًا:

1. **استخراج الإشارات العشر فعليًا** كدوال منفصلة قابلة للاختبار وحدها (Pure functions تأخذ بيانات وتُرجع قيمًا، بدون اعتماد على قاعدة البيانات مباشرة):
   - اسم الملف، اسم المجلد، اسم مجلد المؤلف (parsing حقيقي، ليس افتراض ثابت)
   - اكتشاف نمط السلسلة ورقم الكتاب فيها (مثال: "الاسم - الجزء 3" أو "Book 3")
   - قراءة الـMetadata الداخلية الفعلية (tags الموجودة أصلاً في AudioMetadataReader)
   - المدة الإجمالية، عدد/ترتيب الملفات
   - اسم الراوي (من tag أو من نمط نصي شائع في اسم المجلد/الملف مثل "قراءة"/"روى"/"narrated by")
   - الصيغة/الجودة

2. **خوارزمية Confidence Score حقيقية**: دالة `calculateConfidence(signals: EditionSignals): Float` موثقة بتعليقات تشرح وزن كل إشارة، تُرجع قيمة فعلية بين 0 و1 بناءً على تطابق الإشارات — ليست قيمة ثابتة.

3. **`enum class IntelligenceLevel { CONSERVATIVE, BALANCED, AGGRESSIVE }`** كإعداد فعلي يختاره المستخدم من شاشة Settings، محفوظ محليًا (DataStore أو مشابه)، ويُقرأ فعليًا في منطق القرار أثناء الفحص.

4. **[أهم نقطة]** اكتب دالة منفصلة ومعزولة تمامًا `canAutoMerge(subject: EditionSignals, candidate: EditionSignals, level: IntelligenceLevel): Boolean` تُطبّق القيد الصارم: اختلاف راوٍ واضح أو فرق مدة > 15% → `false` دائمًا، بصرف النظر عن `level`. اكتب اختبار وحدة صريح يستدعي هذه الدالة بالمستويات الثلاثة على نفس حالتين (راويين مختلفين بوضوح) ويثبت أن النتيجة `false` في الحالات الثلاث كلها.

5. **فعّل `EditionMatchDecision` فعليًا**: كل قرار (تلقائي عالي الثقة في Balanced، أو من المستخدم عبر شاشة Review) يُكتب في الجدول عبر `EditionMatchDecisionDao.insert()` (الموجود لكن غير مستخدم حاليًا) بمرجعين صريحين. اكتب منطقًا بسيطًا وحقيقيًا (وليس شكليًا) يستخدم القرارات السابقة لنفس المستخدم لتعديل الأوزان مستقبلاً (مثال بسيط قابل للتحقق: لو المستخدم أكّد سابقًا أن نمط تسمية معيّن = نفس الإصدار، زِد وزن ذلك النمط في القرارات التالية).

6. **User Override Wins بشكل فعلي وليس عرضيًا**: أضف حقل/آلية تُعلِّم أي حقل (عنوان/راوٍ/غلاف/تجميع) عدّله المستخدم يدويًا كـ`isUserConfirmed` على مستوى مناسب، وأضف تحققًا صريحًا في منطق التحديث أثناء أي Scan لاحق يمنع الكتابة فوق الحقول المُعلَّمة، إلا عند طلب "Reset Metadata" صريح على ذلك العنصر تحديدًا. اكتب اختبار وحدة/تكامل: عدّل عنوان كتاب يدويًا، شغّل Scan كامل مرة أخرى، وتأكد أن العنوان لم يتغير.

7. اربط شاشة "Review Matches" فعليًا بالـEditionRepository (لا تزال شاشة UI فقط بدون بيانات حقيقية إن وُجدت) وشاشة إدارة الإصدارات في BookDetails الحقيقية (بعد Phase R1).

توقف بعد كل نقطة، شغّل اختبارات الوحدة الجديدة + كل الاختبارات القديمة (No Regression)، واعرض النتائج الحرفية. النقطة 4 (القيد الصارم) هي الأهم في هذه المرحلة كاملة — لا تنتقل للنقطة التالية قبل إثباتها باختبار فعلي ناجح.
```

---

## Phase R3 — Smart Sleep Timer: إعادة بناء كاملة من الصفر

```
بناءً على قسم "Sleep Timer الذكي — النسخة المُحصّنة والكاملة" في AUDIOBOOK_APP_SPEC.md v2. الكود الحالي (setSleepTimer في ExoPlaybackController) هو عدّاد تنازلي بسيط + pause() فقط — هذا استبدال كامل، وليس إضافة على الموجود:

1. أنشئ `SleepTimerController` كطبقة مستقلة داخل Playback، تتفاعل مع الصوت عبر `PlaybackController` فقط (ليس ExoPlayer مباشرة)، بآلة حالات صريحة:
   `IDLE → RUNNING → WARNING_WINDOW(≤3min) → FADING_OUT → STOPPED`

2. **استخدم Clock/Timer قابل للحقن (Injectable/Fake Clock)** منذ البداية بدلًا من `delay()` الحقيقي مباشرة، لجعل كل نقاط التوقيت (3:00, 2:30, 2:00, 1:30, 1:00, 0:30) قابلة للاختبار الفوري بدون انتظار وقت فعلي في الاختبارات.

3. عند دخول نافذة التحذير (آخر 3 دقائق): نبضة صوتية هادئة (Duck عبر خفض مستوى الصوت مؤقتًا ثم إعادته، وليس مقاطعة كاملة) عند كل نقطة من النقاط الست بالضبط — اكتب اختبار وحدة يثبت حدوث 6 نبضات بالضبط عند التوقيتات الصحيحة تمامًا باستخدام الـFake Clock.

4. حالة `isExtendWindowVisible: Boolean` صريحة ومنفصلة في الحالة المكشوفة للـUI — `false` قبل دخول نافذة الـ3 دقائق، `true` داخلها. اختبار وحدة صريح لكلتا الحالتين.

5. أضف Custom Actions على `MediaSession` (+15/+30/+60 دقيقة) تعمل من الإشعار/شاشة القفل دون فتح التطبيق — هذا يتطلب تعديل `PlaybackService`. وثّق بوضوح إن كان اختبار هذا فعليًا على شاشة قفل حقيقية غير متاح في بيئتك (حسب بروتوكول الإثبات الصادق)، واكتفِ بإثبات أن الـAction مسجّلة وتستدعي المنطق الصحيح عبر اختبار وحدة/instrumented.

6. **عرّف "Active Interaction" كـsealed class/enum صريح** يطابق حرفيًا القائمتين في الـSpec (Seek, Play/Pause صريح, ±15s, Previous/Next Chapter, إضافة Bookmark/Note, إنشاء Chapter, تغيير السرعة, تمديد يدوي = تفعّل التمديد التلقائي؛ فتح الشاشة، بقاء الشاشة مضاءة، حركة الجهاز، إشعار آخر، أو مجرد Foreground = لا تفعّله). اكتب اختبار وحدة لكل بند من القائمتين على حدة.

7. **قاعدة +15 دقيقة بالضبط** (وليس Reset، وليس +30) عند أي تفاعل فعلي أثناء نافذة التحذير — اختبار وحدة يثبت الرقم 15 بالضبط بعدة قيم بداية مختلفة للمؤقت. أضف Toast/رسالة "تم تمديد مؤقت النوم تلقائيًا".

8. Fade Out حقيقي: تدرّج مستوى الصوت (`player.volume`) خلال آخر 2-3 ثوانٍ دون تمديد، ثم توقف كامل + حفظ الموضع فورًا عبر آلية الحفظ الموجودة أصلاً في `ExoPlaybackController`.

9. **عند التوقف الفعلي: اكتب `ListeningSession` جديدة إلى قاعدة البيانات فعليًا** (`endReason = SLEEP_TIMER`, `sessionState = COMPLETED`) — هذا غائب تمامًا حاليًا (الكود الحالي يستدعي `pause()` فقط بلا أي كتابة لقاعدة البيانات). اكتب اختبار تكامل يثبت وجود السجل في قاعدة بيانات Room in-memory بعد محاكاة انتهاء المؤقت.

توقف بعد كل نقطة، شغّل الاختبارات الجديدة (بالـFake Clock) + كل الاختبارات القديمة، واعرض النتائج الحرفية. لا تدّعِ اختبارًا على جهاز/شاشة قفل حقيقية إلا إن كان قد حدث فعلاً وبدليل.
```

---

## Phase R4 — Statistics & History: تنفيذ حقيقي كامل

```
بناءً على قسم "Statistics و History" في AUDIOBOOK_APP_SPEC.md v2. StatisticsRepository الحالي يحتوي دالة واحدة فقط (completedSessions) بلا أي حساب — هذه المرحلة تبني كل المنطق الفعلي:

1. وسّع `StatisticsRepository` بدوال حقيقية:
   - `listeningTimeForRange(range: DateRange): Long` (اليوم/الأسبوع/الشهر) بحساب فعلي من `ListeningSession.durationListenedMs`.
   - `completedBooksCount(): Int`
   - `completedChaptersCount(): Int` — **يجب أن يعتمد على تعريف "Chapter Completed" الحرفي من الـSpec: playhead ≥ 90% من مدة الفصل أو تجاوز نهايته فعليًا.** لا تستخدم أي عتبة أخرى، وثّق الرقم 90% كثابت مسمى صراحة في الكود (وليس رقمًا سحريًا مبعثرًا).
   - `currentStreak(): Int` — أيام متتالية بها جلسة استماع واحدة على الأقل، بغض النظر عن مدتها. اكتب هذا كخوارزمية صريحة على قائمة تواريخ الجلسات مرتبة، وليس تقريبًا.
   - `averageSpeed(): Float`

2. **راقب اكتمال الفصل أثناء التشغيل الفعلي**، ليس بحسابٍ لاحق تقريبي فقط: أضف مراقبًا (Observer/Listener) على موضع التشغيل في `PlaybackController`/`ExoPlaybackController` يتحقق عند كل تحديث موضع من نسبة تقدم الفصل الحالي، ويسجل اكتمال الفصل بمجرد تجاوز 90% (مرة واحدة فقط لكل فصل، لا تكرار).

3. اربط شاشتي Statistics وHistory الحقيقيتين (لا توجد شاشتان فعليتان حاليًا) بهذه الدوال عبر ViewModel مخصص.

4. اكتب اختبارات وحدة:
   - سيناريو جلسات بتواريخ متتالية (بدون فجوة) → Streak صحيح.
   - سيناريو بفجوة يوم واحد → Streak ينكسر وتُعاد البداية من جديد.
   - سيناريو استماع يتجاوز 90% بالضبط من فصل → يُسجَّل كمكتمل.
   - سيناريو استماع يصل 89% فقط → لا يُسجَّل كمكتمل (اختبار سلبي صريح، لا يكفي اختبار الحالة الإيجابية فقط).

توقف بعد كل نقطة، شغّل الاختبارات الجديدة + كل القديمة، واعرض النتائج الحرفية — خصوصًا نتائج اختبار Streak وحدَّي الـ89%/90% لأنهما أكثر نقطة عرضة للخطأ الحسابي في هذه المرحلة.
```

---

## Phase R5 — Accessibility: تنفيذ حقيقي وليس ادعاءً

```
بناءً على قسم "إمكانية الوصول" في AUDIOBOOK_APP_SPEC.md v2. تم التحقق أن contentDescription غير مستخدم إطلاقًا في المشروع (0 نتيجة بحث نصي) — هذه مراجعة شاشة بشاشة إلزامية، وليست إضافة سطحية:

1. راجع كل شاشة (Library, BookDetails, Player, Bookmarks, LibraryRoots, DesignSystemShowcase) وأضف `contentDescription` حقيقيًا ووصفيًا (ليس نصًا عامًا مثل "زر") لكل `Icon`/`IconButton`/عنصر تفاعلي بلا نص مرئي مصاحب.
2. تأكد من مساحات لمس ≥48dp لكل عنصر قابل للنقر عبر `Modifier.sizeIn(minWidth = 48.dp, minHeight = 48.dp)` أو الاعتماد الصريح على `LocalMinimumInteractiveComponentSize` في Material3.
3. تأكد أن كل النصوص تستخدم `sp` (وليس `dp`) للأحجام، وأن التخطيطات تستخدم `weight`/`wrapContent` بدل ارتفاعات/عروض ثابتة بالبكسل حيثما أمكن، لدعم تكبير الخط دون كسر.
4. اكتب اختبارات Compose UI حقيقية (باستخدام بنية Phase R0):
   - `onNodeWithContentDescription("...")` لكل عنصر تفاعلي رئيسي في كل شاشة، يثبت وجوده فعليًا في شجرة الـsemantics.
   - اختبار بتكبير `fontScale` (عبر `LocalDensity`/`LocalConfiguration` في بيئة الاختبار) على الشاشات الرئيسية، يتحقق من عدم وجود تراكب/قطع نصوص (بقدر ما تسمح أدوات الاختبار الآلي، ووثّق حدود ما يمكن التحقق منه آليًا مقابل ما يحتاج مراجعة بصرية يدوية).
5. **لا تدّعِ "تشغيل TalkBack فعليًا"** ما لم يحدث ذلك بدليل تشغيل حقيقي على جهاز/محاكي. البديل الصادق المقبول هنا: اختبارات semantics tree عبر Compose Testing APIs، مُصرَّح عنها بهذا الاسم بالتحديد.

توقف بعد كل شاشة، اعرض قائمة العناصر التي أُضيف لها contentDescription + نتائج اختبارات semantics الفعلية.
```

---

## Phase R6 — مراجعة الانحدار الشاملة والتقرير الختامي الصادق

```
هذه المرحلة الأخيرة تُنهي خطة الإصلاح بتقرير حقيقي قابل للتدقيق، وليس تأكيدًا عامًا:

1. شغّل **كل** الاختبارات المتراكمة من Phase R0 حتى R5، بالإضافة لكل اختبارات الوحدة الأصلية (13 ملفًا من Phase 0–12 الأصلية)، في تشغيل واحد كامل (`./gradlew test` + أي instrumented tests إن توفرت بيئة تشغيلها)، وأرفق المخرجات الحرفية الكاملة (عدد الاختبارات، الناجح، الفاشل، إن وُجد).

2. أعد اختبار صريح ومباشر لكل قيد حرج ورد في الـSpec الأصلي، وأثبت أنه لم يُكسر تراكميًا خلال مراحل R1–R5:
   - User Override Wins (R2)
   - القيد الصارم لمنع الدمج التلقائي عند اختلاف الراوي/المدة >15% (R2)
   - نافذة التحذير 3 دقائق + زر التمديد المشروط (R3)
   - قاعدة +15 دقيقة بالضبط (R3)
   - تعريف Chapter Completed 90% بالضبط (R4)
   - سياسة الملفات المفقودة (كانت منفَّذة أصلاً — تأكد أنها لم تتأثر بإعادة هيكلة R1/R2)
   - Gradient الديناميكي بأولوياته الأربع (كان منفَّذًا أصلاً — تأكد من عدم كسره بربط بيانات حقيقية في R1)

3. اكتب تقريرًا ختاميًا بجدول من عمودين لكل قرار MUST في `AUDIOBOOK_APP_SPEC.md v2`: **"الحالة الفعلية"** و**"نوع الدليل"** (unit test / instrumented فعلي / مراجعة كود / غير مُنجز)، بدون أي عبارة "تم التأكد" أو "يعمل بشكل ممتاز" بلا دليل مرفق بجانبها مباشرة.

4. أي بند يظهر في الجدول بلا دليل كافٍ يُصنَّف صراحة "غير مكتمل" مهما كان صغيرًا — هذا التقرير هو معيار الحكم النهائي على جاهزية الإصدار الأول للمنتج.

لا تُغلق هذه المرحلة، ولا المشروع، دون هذا الجدول الكامل والصادق.
```

---

## ملاحظة ختامية

الفرق الجوهري بين هذه الخطة والمراحل الأصلية (Phase 0–12) هو: **كل ادعاء اختبار من الآن فصاعدًا يجب أن يكون قابلاً للتحقق فعليًا** (كود اختبار + أمر تشغيل + مخرجات)، وليس وصفًا سرديًا لما "تم التأكد منه". هذا هو الدرس المستفاد من التدقيق: الفجوة الحقيقية في هذا المشروع لم تكن فقط في الكود المفقود، بل في تقارير تنفيذ سابقة زعمت اختبارًا فعليًا لم يكن ممكنًا حدوثه أصلاً بسبب غياب بنية `androidTest` بالكامل.
